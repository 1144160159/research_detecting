primary_queries:
  - "encrypted traffic anomaly detection open set"
  - "malicious encrypted traffic detection unknown class"
  - "self-supervised encrypted traffic anomaly detection"
  - "multimodal encrypted traffic intrusion detection"
  - "open world network intrusion detection encrypted traffic"

expansion_queries:
  open_set:
    - "open-set network traffic classification"
    - "unknown class detection network traffic"
    - "out-of-distribution detection intrusion detection"
    - "reject option traffic classification"
  self_supervised:
    - "self-supervised learning encrypted traffic classification"
    - "contrastive learning network traffic anomaly detection"
    - "masked autoencoder encrypted traffic"
  multimodal:
    - "multimodal network traffic analysis"
    - "packet flow byte sequence multimodal traffic classification"
    - "graph sequence multimodal intrusion detection"
  intrusion:
    - "encrypted malicious traffic intrusion detection"
    - "malware traffic detection TLS QUIC"
    - "botnet encrypted traffic detection"

keyword_groups:
  open_set:
    weight: 5
    terms:
      - open-set
      - open set
      - open-world
      - open world
      - unknown class
      - unknown attack
      - unseen class
      - novel class
      - zero-day
      - out-of-distribution
      - OOD
      - reject option
  encrypted_traffic:
    weight: 4
    terms:
      - encrypted traffic
      - TLS
      - SSL
      - HTTPS
      - QUIC
      - VPN
      - Tor
      - traffic classification
      - network flow
      - packet
  anomaly_detection:
    weight: 3
    terms:
      - anomaly detection
      - anomalous
      - abnormal
      - outlier
      - novelty detection
      - one-class
      - intrusion detection
  malicious:
    weight: 3
    terms:
      - malicious traffic
      - malware
      - botnet
      - attack detection
      - intrusion
      - C2
      - command and control
      - ransomware
  self_supervised:
    weight: 3
    terms:
      - self-supervised
      - contrastive learning
      - masked
      - autoencoder
      - pretraining
      - representation learning
  multimodal:
    weight: 3
    terms:
      - multimodal
      - multi-modal
      - multiple modalities
      - packet-level
      - flow-level
      - byte sequence
      - graph
  boundary:
    weight: 2
    terms:
      - decision boundary
      - boundary
      - threshold
      - confidence
      - calibration
      - margin
      - hypersphere
      - prototype

