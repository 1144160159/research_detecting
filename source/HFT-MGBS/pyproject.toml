[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hft-mgbs"
version = "0.1.0"
description = "High-speed full-traffic multi-granularity feature extraction with adaptive budget scheduling"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["hft_mgbs*"]
