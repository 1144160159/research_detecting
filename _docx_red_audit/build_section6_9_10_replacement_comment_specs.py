from __future__ import annotations

import argparse
import json
import re
from pathlib import Path


MATH_RE = re.compile(r"\[\[MATH:(.*?)\]\]")


DELETE_IDS = {
    # Section 6: retain M066 as the whole-section replacement comment.
    "M067", "S23", "M072", "M071", "M070", "M069", "M068", "S09",
    "M073", "M074", "M075", "M076", "M079", "M078", "M077", "M080",
    "M081", "S24", "M082", "M083", "M084",
    # Section 9: retain S16 as the whole-section replacement comment.
    "M088", "M089", "M090", "M091", "M092", "M093", "M095", "M094",
    "M096", "M097", "M098", "S12", "M099", "M100", "M101", "M102",
    "S13", "M103", "M104", "M105", "M106", "M107",
    # Section 10: retain S15 as the whole-section replacement comment.
    "S14", "M108", "M109", "M110", "M111", "M112", "M113", "M114",
    "M115", "M116", "M130", "M129", "M128", "M127", "M126", "M125",
    "M124", "M123", "M122", "M121", "M120", "M119", "M118", "M117",
}


SECTION6_TEXT = """[问题066/154：路径调度模块整体替换] 建议将“6、路径调度模块”标题及其后全部内容整体替换为以下内容，替换范围截至“7、流量大模型受控生成模块”标题之前，不替换第7节标题：
6、路径调度模块
路径调度模块用于根据当前告警的检测风险、证据覆盖程度、检测不确定性、负证据压力、预计幻觉风险和可用计算预算，为告警选择与其证据状态相匹配的处理路径。该模块不直接改变恶意流量检测模块输出的检测标签和风险分数，而是决定是否调用流量大模型、是否先补充证据、采用何种校验强度以及在校验失败时采用何种降级输出方式。

对于告警[[MATH:a]]，路径调度模块读取状态向量：
[[MATH:x_a=(R_f,Cov(a),U_f,P_neg(a),B,Miss(a))]]
其中，[[MATH:R_f]]为恶意流量检测模块输出并经告警标准化后的融合检测风险，取值范围为[[MATH:0≤R_f≤1]]；[[MATH:Cov(a)]]为第5节计算的证据覆盖率；[[MATH:U_f]]为恶意流量检测模块输出的归一化不确定性，取值范围为[[MATH:0≤U_f≤1]]，其数值越大表示当前检测结果越不稳定；[[MATH:Miss(a)]]为尚未满足最低数量要求的必要证据类型集合；[[MATH:B=(B_tok,B_lat,B_call)]]为当前告警可用预算，其中[[MATH:B_tok]]、[[MATH:B_lat]]和[[MATH:B_call]]分别表示允许使用的最大输入输出标记数量、最大处理时延和最大模型调用次数。

负证据压力[[MATH:P_neg(a)]]用于表示当前证据包中削弱恶意结论的反向证据强度。令[[MATH:E_neg(a)]]为第3节得到且第5节保留的负证据集合，令[[MATH:w_i^neg=rel_i trust_i fresh_i]]为负证据[[MATH:e_i]]的有效权重，则：
[[MATH:P_neg(a)=(∑_(e_i∈E_neg(a))w_i^neg neg_i)/(∑_(e_i∈E_neg(a))w_i^neg+ε)]]
其中，[[MATH:neg_i]]为第3节和第5节定义的负证据强度，[[MATH:ε]]为防止分母为0的极小正数。当[[MATH:E_neg(a)=∅]]时令[[MATH:P_neg(a)=0]]。由此，“负证据压力”不是人工给出的等级，而是对当前告警中有效负证据强度的加权平均。

系统设置五种候选路径：
[[MATH:P={p_L,p_G,p_S,p_V,p_D}]]
其中，[[MATH:p_L]]为轻量模板路径，[[MATH:p_G]]为完整受控生成路径，[[MATH:p_S]]为补证后重调度路径，[[MATH:p_V]]为高强度校验生成路径，[[MATH:p_D]]为降级证据输出路径。各路径的具体执行方式如下。

轻量模板路径[[MATH:p_L]]不调用流量大模型。系统按照固定字段顺序输出告警编号、检测标签、融合检测风险、已有流量证据、已有模型证据、证据缺失类型和“不形成确定性归因”的限制说明。该路径适用于[[MATH:Cov(a)<0.50]]、缺少有效流量证据或模型证据、告警字段不完整以及预算无法满足一次受控生成的情况。

完整受控生成路径[[MATH:p_G]]将第5节排序压缩后的证据包、证据编号和输出模式送入第7节，在预算内执行一次证据约束生成，并调用第9节的结构校验、引用校验、支持度校验和一致性校验。该路径适用于[[MATH:Cov(a)≥0.70]]、必要的流量证据和模型证据均存在、负证据压力未达到强冲突阈值且预算满足生成与校验开销的情况。

补证后重调度路径[[MATH:p_S]]不立即生成结论，而是针对[[MATH:Miss(a)]]逐项执行确定性的证据检索操作，包括扩大历史检索窗口、读取告警关联流的可复算摘要、补取同源或同目的对象的历史记录以及重新查询资产快照。补证完成后重新执行第3节的统一证据对象转换、第5节的排序压缩和覆盖率计算，再返回本模块重新选择路径。单个告警最多执行一次补证循环，防止持续检索占用预算。该路径适用于[[MATH:0.50≤Cov(a)<0.70]]且缺失证据能够从已配置的数据源中检索得到的情况。

高强度校验生成路径[[MATH:p_V]]使用与[[MATH:p_G]]相同的证据约束生成方式，但强制逐条输出关键结论及其证据编号，并执行第9节的全部校验项；对于未见IOC、证据不足的攻击归因、风险越界和负证据冲突，不允许通过其他校验得分抵消。系统最多允许一次基于校验失败项的约束重生成，重生成时不得增加证据包中不存在的事实。该路径适用于[[MATH:R_f≥0.80]]、[[MATH:U_f≥0.50]]、[[MATH:P_neg(a)≥0.40]]，或者输出字段包含IOC、攻击归因和确定性风险结论中的任意一项的情况。

降级证据输出路径[[MATH:p_D]]用于处理生成或校验后的不可修复失败。系统删除无有效引用、支持度不足、未见IOC、归因条件不满足或风险越界的结论，仅保留检测结果、可验证证据、负证据、校验失败原因和证据不足说明。该路径始终作为可行的兜底路径，不再次调用流量大模型。

在计算路径效用之前，系统先执行可行性门控。令[[MATH:F(a,p)∈{0,1}]]表示路径[[MATH:p]]对告警[[MATH:a]]是否可行。对于需要模型调用的路径，只有必要输入字段存在且预计标记数量、时延和调用次数分别不超过对应预算时，才令[[MATH:F(a,p)=1]]；对于补证路径，只有[[MATH:Miss(a)]]中的至少一种证据存在可用检索源且尚未执行过补证循环时，才令[[MATH:F(a,p_S)=1]]；轻量模板路径和降级证据输出路径不受模型调用预算限制。若路径不可行，则不参与效用比较。

对于可行路径，系统分别计算预计收益、预算代价、预计幻觉风险和证据缺口。路径[[MATH:p]]的预计收益为：
[[MATH:G(a,p)=0.40ΔCov(a,p)+0.35P_pass(a,p)+0.25I_keep(a,p)]]
其中，预计覆盖率增量为：
[[MATH:ΔCov(a,p)=max(0,Cov_p(a)-Cov(a))]]
[[MATH:Cov_p(a)]]为执行路径[[MATH:p]]后预计达到的覆盖率。[[MATH:P_pass(a,p)]]为同一模型版本、覆盖率区间和风险区间内，该路径历史样本通过第9节强校验的比例，采用加一平滑计算：
[[MATH:P_pass(a,p)=(N_pass(a,p)+1)/(N_path(a,p)+2)]]
其中，[[MATH:N_pass(a,p)]]为验证集或稳定运行窗口中通过强校验的同状态样本数量，[[MATH:N_path(a,p)]]为选择该路径的同状态样本总数。[[MATH:I_keep(a,p)]]为路径执行后保留的高价值证据权重占全部高价值候选证据权重的比例；高价值证据是指第5节中[[MATH:Score(e_i)≥θ_e]]的模型证据、流量证据和负证据。当不存在高价值候选证据时令[[MATH:I_keep(a,p)=1]]。因此，[[MATH:G(a,p)]]同时反映补证收益、历史校验效果和关键证据保留效果。

路径预算代价为：
[[MATH:C(a,p)=(c_tok(a,p)+c_lat(a,p)+c_call(a,p))/3]]
其中：
[[MATH:c_tok(a,p)=min(1,n_tok(a,p)/(B_tok+ε))]]
[[MATH:c_lat(a,p)=min(1,t_lat(a,p)/(B_lat+ε))]]
[[MATH:c_call(a,p)=min(1,n_call(a,p)/(B_call+ε))]]
[[MATH:n_tok(a,p)]]、[[MATH:t_lat(a,p)]]和[[MATH:n_call(a,p)]]分别由路径模板的历史统计中位数估计。一个实施例中，单告警预算取[[MATH:B_tok=4096]]、[[MATH:B_lat=3000ms]]、[[MATH:B_call=2]]；具体取值可根据部署设备和实时性要求调整。

路径预计幻觉风险为：
[[MATH:H(a,p)=max(0,min(1,0.30(1-Cov_p(a))+0.25U_f+0.20P_neg(a)+0.15I_IOC(p)+0.10I_attr(p)-0.20P_val(a,p)))]]
其中，[[MATH:I_IOC(p)]]表示路径输出模式是否允许生成IOC字段，允许时取1，否则取0；[[MATH:I_attr(p)]]表示是否允许生成攻击归因字段；[[MATH:P_val(a,p)]]为该路径在同状态历史样本中通过未见IOC校验、归因校验和风险越界校验的比例。该式使覆盖率不足、检测不确定性较高和负证据较强时的预计幻觉风险升高，使经过高强度校验且历史通过率较高的路径风险降低。

路径证据缺口为：
[[MATH:D(a,p)=(∑_(t∈T)w_t max(0,m_t-n_t^p(a)))/(∑_(t∈T)w_t m_t+ε)]]
其中，[[MATH:T]]、[[MATH:w_t]]和[[MATH:m_t]]沿用第5节定义，[[MATH:n_t^p(a)]]为路径[[MATH:p]]执行后预计保留的类型[[MATH:t]]有效证据数量。该式直接量化各必要证据类型距最低数量要求的差距；所有最低数量均已满足时，[[MATH:D(a,p)=0]]。

路径效用函数为：
[[MATH:J(a,p)=G(a,p)-λC(a,p)-ρH(a,p)-ηD(a,p)]]
最终路径为：
[[MATH:p^*=argmax_(p∈P,F(a,p)=1) J(a,p)]]
一个实施例中取[[MATH:λ=0.25]]、[[MATH:ρ=0.35]]、[[MATH:η=0.25]]。上述参数通过验证集网格搜索确定，优化目标是在模型调用预算内提高强校验通过率和高价值证据保留率，同时降低平均时延、未见IOC率和无证据结论率；参数仅使用训练集或验证集确定，不利用待检测样本的真实标签调整。

前述覆盖率区间、风险区间和输出字段条件仅用于确定路径是否可行以及触发不可绕过的安全约束，效用函数用于在全部可行路径之间选择预计效果最优的路径，两者不存在重复判定。若不存在可行的模型生成路径，系统直接选择[[MATH:p_L]]；若生成后出现不可修复的强校验失败，系统强制选择[[MATH:p_D]]，不再以效用分数覆盖该兜底规则。路径调度模块最终输出所选路径、路径效用、预算分配、触发条件和参数版本，供后续生成、校验和反馈更新模块追溯。"""


SECTION9_TEXT = """[补充事项16/26：幻觉校验模块整体替换] 建议将“9、幻觉校验模块”标题及其后全部内容整体替换为以下内容，替换范围截至“10、可信度融合与输出控制模块”标题之前，不替换第10节标题：
9、幻觉校验模块
幻觉校验模块用于对第7节产生的结构化生成结果执行确定性检查和证据支持检查，识别结构缺失、无效引用、证据不足、未见IOC、无依据归因、内部矛盾和风险越界。该模块以第3节形成并经第5节筛选的证据包为唯一事实边界，不允许通过生成文本自身补充或证明证据包中不存在的事实。

第7节输出结构化生成对象：
[[MATH:Y_a=(summary,{c_j},IOC_out,Attr_out,r_gen,Insuff)]]
其中，[[MATH:summary]]为告警摘要；[[MATH:{c_j}]]为关键结论集合；[[MATH:IOC_out]]为生成结果中的IOC集合；[[MATH:Attr_out]]为归因对象及归因类型；[[MATH:r_gen]]为生成风险状态对应的数值；[[MATH:Insuff]]为证据不足说明。关键结论不是在生成后由人工提取，而是第7节通过固定输出模式要求流量大模型写入[[MATH:key_conclusions]]字段。第[[MATH:j]]条关键结论表示为：
[[MATH:c_j=(CID_j,text_j,type_j,v_j,R_j,conf_j)]]
其中，[[MATH:CID_j]]为结论编号，[[MATH:text_j]]为结论文本，[[MATH:type_j]]为行为、IOC、归因或风险类型，[[MATH:v_j∈{1,2,3}]]为低、中、高三个重要等级，[[MATH:R_j]]为该结论引用的证据编号集合，[[MATH:conf_j]]为生成阶段给出的自评置信度。证据编号均来自证据包中已经存在的[[MATH:EID]]字段；输出模式要求每条关键结论单独列出引用编号，未列出编号的结论按无有效引用处理。

第一步，执行结构校验。系统检查告警索引、摘要、关键结论列表、引用编号列表、证据不足字段和生成版本是否存在，检查字段类型和数量是否符合固定模式，并检查每个[[MATH:CID_j]]是否唯一。任一必要字段缺失、字段类型错误或对象无法解析时，令结构失败标志[[MATH:b_struct=1]]；否则令[[MATH:b_struct=0]]。结构失败时不直接接受自由文本，而是按照固定模式重新解析一次；仍失败则进入第10节的降级证据输出。

第二步，执行证据引用校验。令[[MATH:E(a)]]为当前告警证据包，令[[MATH:ID(E(a))]]为其中全部有效证据编号集合。对于结论[[MATH:c_j]]，有效引用集合为：
[[MATH:R_j^valid={EID∈R_j:EID∈ID(E(a))}]]
系统同时检查被引用证据的告警索引是否等于当前[[MATH:AID]]、证据版本是否属于本次冻结的证据包版本、原始数据索引是否可回溯。引用编号不存在、指向其他告警、版本不一致或原始索引失效时，该引用记为无效。结论无有效引用时令其引用失败；全文存在任一高重要等级结论无有效引用，或者存在伪造证据编号时，令[[MATH:b_ref=1]]。

第三步，执行逐结论支持度校验。对结论[[MATH:c_j]]，引用支持集合为：
[[MATH:R_j^+={e_i∈E(a):EID_i∈R_j^valid,neg_i<0.50,M_i^contra=0}]]
引用冲突集合为：
[[MATH:R_j^-={e_i∈E(a):EID_i∈R_j^valid,neg_i≥0.50∨M_i^contra=1}]]
其中，[[MATH:R_j^+]]可以包含流量证据、模型证据、资产证据和历史证据；[[MATH:R_j^-]]主要包含负证据，也包括字段值或检测标签与结论互斥的其他证据。[[MATH:M_i^contra]]为确定性冲突指示量：当规范化字段与结论中的同名字段取值互斥、历史确认标签与当前结论相反，或者证据明确命中第3节定义的负向条件时取1，否则取0。

证据[[MATH:e_i]]对结论[[MATH:c_j]]的正向匹配度为：
[[MATH:s_ij=0.40M_ij^field+0.35M_ij^sem+0.25M_ij^label]]
其中，[[MATH:M_ij^field]]为结论中可解析的地址、端口、协议、域名、证书摘要、规则编号和时间范围与证据字段的一致字段权重占比；[[MATH:M_ij^sem]]为结论文本向量与证据摘要向量余弦相似度映射后的数值，并满足[[MATH:0≤M_ij^sem≤1]]；[[MATH:M_ij^label]]为结论行为类别与证据检测标签的一致程度，标签相同时取1，标签互斥时取0，底层分类器提供类别概率时取对应类别概率。某一分量不存在时删除该分量并对其余权重重新归一化。

证据[[MATH:e_i]]对结论[[MATH:c_j]]的冲突强度为：
[[MATH:g_ij=0.60neg_i+0.40M_i^contra]]
令有效权重为[[MATH:ω_ij=rel_i trust_i fresh_i]]。结论支持度为：
[[MATH:Sup(c_j)=max(0,min(1,κ(∑_(e_i∈R_j^+)ω_ij s_ij)/(∑_(e_i∈R_j^+)ω_ij+ε)-ξ(∑_(e_i∈R_j^-)ω_ij g_ij)/(∑_(e_i∈R_j^-)ω_ij+ε)))]]
其中，一个实施例取[[MATH:κ=1.00]]、[[MATH:ξ=0.80]]，表示正向证据按原强度计入，负证据冲突按0.80的系数扣减。参数通过验证集搜索确定，使无证据结论识别率提高且有效结论误删率处于允许范围。若[[MATH:R_j^+=∅]]，则正向支持项取0；若[[MATH:R_j^-=∅]]，则冲突项取0。

实施例中将[[MATH:τ_sup=0.65]]作为普通关键结论的最低支持度，将[[MATH:τ_attr=0.80]]作为攻击归因结论的最低支持度。若[[MATH:Sup(c_j)<τ_sup]]，则该结论记为支持度失败；任一高重要等级结论支持度失败时令[[MATH:b_sup=1]]。上述阈值通过验证集上“无证据结论召回率不低于95%且有效结论保留率不低于90%”的约束搜索得到，具体数值属于实施例参数。

第四步，执行未见IOC校验。系统从证据包中的地址、域名、统一资源定位符、文件哈希、证书指纹和协议指纹字段构造可见IOC集合[[MATH:IOC_visible]]，每个可见IOC均绑定至少一个来源证据编号。系统从结构化输出的[[MATH:ioc_list]]字段以及[[MATH:summary]]、[[MATH:key_conclusions]]和[[MATH:attribution]]字段中，采用固定解析器提取IP地址、域名、统一资源定位符、哈希值、证书指纹和协议指纹，构造[[MATH:IOC_out]]。IP地址统一压缩或展开为标准表示，域名转换为小写并删除末尾点，统一资源定位符统一协议名和主机名，哈希值转换为小写，证书指纹删除分隔符后再比较。

未见IOC集合为：
[[MATH:IOC_unseen=IOC_out-IOC_visible]]
若[[MATH:IOC_unseen≠∅]]，说明生成结果包含证据包中未出现且无法回溯的IOC，令[[MATH:b_ioc=1]]，并将对应IOC及引用该IOC的结论全部删除或要求在同一证据包约束下重生成。未见IOC属于不可被其他得分抵消的强失败项。

第五步，执行归因校验。归因校验在结构化草稿生成后进行，系统直接读取[[MATH:Attr_out]]字段，并从相应归因结论的[[MATH:R_j^valid]]中统计独立证据类型集合[[MATH:T_attr(c_j)]]。只有同时满足：
[[MATH:|T_attr(c_j)|≥2]]
[[MATH:Sup(c_j)≥τ_attr]]
[[MATH:R_j^- =∅]]
且两类证据来自不同采集或计算来源时，才允许保留确定性归因表述。模型标签和由该模型标签直接派生的特征贡献视为同一来源，不能重复计数。条件不满足时令[[MATH:b_attr=1]]，系统将归因结论改写为“证据不足，不能形成确定性归因”，而不是提交人工审查。

第六步，执行一致性校验。系统将摘要、各关键结论、IOC列表、归因字段和风险状态转换为规范化命题，检查同一字段是否出现互斥取值，检查摘要中的检测标签是否与结构化标签一致，检查声称“不存在异常”时是否仍给出高风险结论，检查负证据说明是否被摘要遗漏，并检查同一IOC在不同字段中的类型是否一致。存在任一无法通过删除重复文本修复的逻辑矛盾时令[[MATH:b_cons=1]]。

第七步，执行风险越界校验。生成风险状态限定为“正常”“可疑”“高风险”和“已确认”四级，并映射为：
[[MATH:M_r(正常)=0.10,M_r(可疑)=0.40,M_r(高风险)=0.70,M_r(已确认)=0.90]]
令[[MATH:r_gen=M_r(status_gen)]]为生成风险值，[[MATH:R_f]]为底层检测器输出的融合检测风险。风险越界量为：
[[MATH:O_r=max(0,r_gen-R_f-ε_r)]]
一个实施例取[[MATH:ε_r=0.15]]，用于容纳风险等级离散化造成的量化误差。若[[MATH:O_r>0]]，说明生成文本给出的风险强度超过检测结果及允许容差，令[[MATH:b_over=1]]，删除或降低相应风险结论，并将[[MATH:O_r]]传递给第10节作为可信度扣减项。底层检测风险[[MATH:R_f]]仅作为风险越界基准，不作为生成解释可信度的正向加分项。

各校验失败标志组成：
[[MATH:b=(b_struct,b_ref,b_sup,b_ioc,b_attr,b_cons,b_over)]]
校验模块按照“首次校验、确定性修复、再次校验”的顺序执行。确定性修复包括删除无效引用、删除未见IOC、删除支持度不足结论、将不满足条件的归因改为证据不足说明以及将越界风险降低至允许等级。只有结构完整且证据包未变化时，才允许进行一次约束重生成；再次校验仍存在未见IOC、伪造引用、无支持的高重要等级结论、归因失败或风险越界时，直接进入降级证据输出路径。模块最终输出修复后的结构化结果、逐结论支持度、有效引用集合、未见IOC集合、风险越界量、七项失败标志和校验规则版本，供第10节融合可信度并控制输出。"""


SECTION10_TEXT = """[补充事项15/26：可信度融合与输出控制模块整体替换] 建议将“10、可信度融合与输出控制模块”标题及其后全部内容整体替换为以下内容，替换范围截至“11、反馈更新模块”标题之前，不替换第11节标题：
10、可信度融合与输出控制模块
可信度融合与输出控制模块用于将底层检测器的校准可靠性、证据覆盖率、逐结论支持度、引用质量、检测不确定性、幻觉校验失败程度和风险越界量融合为生成解释可信度，并依据不可绕过的强校验条件决定输出完整解释、受限解释或仅证据输出。本模块将“检测风险”和“解释可信度”分开：[[MATH:R_f]]表示流对象具有恶意风险的程度，数值较高并不意味着生成文本必然可信；因此，[[MATH:R_f]]仅用于第6节的处理优先级和第9节的风险越界比较，不作为本节可信度的正向加分项。

底层检测器校准可靠性[[MATH:C_det]]由当前模型版本在独立验证集上的分箱校准结果得到。系统将检测器输出风险划分为若干固定区间，找到[[MATH:R_f]]所在区间[[MATH:h]]，令[[MATH:N_h^correct]]为该区间内检测标签与实验标签一致的样本数，[[MATH:N_h]]为该区间样本总数，则：
[[MATH:C_det=(N_h^correct+1)/(N_h+2)]]
该式采用加一平滑，避免样本较少时可靠性直接取0或1。若当前模型版本尚无足够验证样本，则使用相邻风险区间合并后的统计值；仍不足时使用该模型版本在全部验证样本上的平滑准确率。[[MATH:C_det]]是模型版本和风险分箱层面的历史校准可靠性，不等于[[MATH:R_f]]，也不由当前样本的生成文本决定。

平均结论支持度[[MATH:S_avg]]由第9节逐结论支持度按结论重要等级加权得到：
[[MATH:S_avg=(∑_(c_j∈C_a)v_j Sup(c_j))/(∑_(c_j∈C_a)v_j+ε)]]
其中，[[MATH:C_a]]为修复后保留的关键结论集合，[[MATH:v_j∈{1,2,3}]]分别对应低、中、高重要等级；若[[MATH:C_a=∅]]，则令[[MATH:S_avg=0]]。因此，高重要等级结论缺乏证据支持时会对整体可信度产生更大影响，普通描述性语句不会与核心检测结论等权计算。

引用质量[[MATH:Q_ref]]用于衡量生成结果中的引用是否真实存在、是否属于当前告警、是否来自冻结的证据包版本以及是否能够回溯原始数据。对于第[[MATH:r]]个引用，定义：
[[MATH:q_r^ref=(I_r^exist+I_r^aid+I_r^ver+I_r^trace)/4]]
其中，[[MATH:I_r^exist]]表示证据编号是否存在，[[MATH:I_r^aid]]表示关联告警索引是否等于当前[[MATH:AID]]，[[MATH:I_r^ver]]表示证据版本是否与当前证据包版本一致，[[MATH:I_r^trace]]表示原始数据索引是否可访问；条件满足时取1，否则取0。整体引用质量为：
[[MATH:Q_ref=(∑_(r=1)^N_ref q_r^ref)/(N_ref+ε)]]
若[[MATH:N_ref=0]]，则令[[MATH:Q_ref=0]]。引用质量与第5节的覆盖率含义不同：[[MATH:Cov(a)]]判断必要证据类型是否齐全，[[MATH:Q_ref]]判断生成结果是否正确引用已经存在的证据。

检测不确定性沿用恶意流量检测模块输出的[[MATH:U_f]]。若分类分支和原型距离异常分支均存在，则先对二者的不确定性进行验证集校准，再按两个分支的有效可靠度归一化加权，得到[[MATH:0≤U_f≤1]]；[[MATH:U_f]]越大表示检测结果越不稳定，在本节中作为扣减项使用。

幻觉校验失败程度由第9节七项失败标志计算：
[[MATH:F_hall=0.10b_struct+0.15b_ref+0.15b_sup+0.20b_ioc+0.15b_attr+0.10b_cons+0.15b_over]]
其中，结构失败、引用失败、支持度失败、未见IOC失败、归因失败、一致性失败和风险越界失败的权重依次为0.10、0.15、0.15、0.20、0.15、0.10和0.15。权重总和为1；未见IOC权重最高，是因为该类错误直接引入证据包之外的可识别事实。若某输出模式不允许生成IOC或归因字段，则对应校验项记为不适用，删除该项并将其余权重重新归一化，而不是直接记为通过。

生成解释可信度为：
[[MATH:Conf(a)=max(0,min(1,θ_1 C_det+θ_2 Cov(a)+θ_3 S_avg+θ_4 Q_ref-θ_5 U_f-θ_6 F_hall-θ_7 O_r))]]
其中，[[MATH:θ_1]]为底层检测器校准可靠性权重，[[MATH:θ_2]]为必要证据类型覆盖权重，[[MATH:θ_3]]为逐结论证据支持权重，[[MATH:θ_4]]为引用可回溯质量权重，[[MATH:θ_5]]为检测不确定性惩罚权重，[[MATH:θ_6]]为幻觉校验失败惩罚权重，[[MATH:θ_7]]为生成风险越界惩罚权重。一个实施例取：
[[MATH:(θ_1,θ_2,θ_3,θ_4,θ_5,θ_6,θ_7)=(0.30,0.20,0.30,0.20,0.10,0.25,0.15)]]
前四项正向权重之和为1，后三项为额外惩罚系数。各参数通过独立验证集的约束网格搜索确定，目标是在保持有效解释保留率的同时降低未见IOC率、无有效引用率和无证据结论率。参数更新时记录训练数据范围、模型版本、证据规则版本和阈值版本，不利用待检测样本的真实标签进行在线调参。

为避免多个相关指标重复放大可信度，系统在参数确定前计算[[MATH:Cov(a)]]、[[MATH:S_avg]]和[[MATH:Q_ref]]在验证集上的相关系数。若任意两项的绝对相关系数大于0.90，则保留与无证据结论识别效果更相关的一项，或者降低两项权重并重新搜索，使其权重和不超过替换前较高者的权重。由此，覆盖率、支持度和引用质量分别反映“证据类型是否齐全”“结论是否被证据支持”和“引用是否真实可回溯”，同时限制高度共线时的重复计分。

除数值可信度外，系统设置强校验通过标志[[MATH:StrongPass(a)]]。只有同时满足以下条件时才令[[MATH:StrongPass(a)=1]]：结构完整；全部高重要等级结论至少具有一个有效引用；全部高重要等级结论满足支持度要求且不存在未见IOC，即[[MATH:Sup(c_j)≥τ_sup,IOC_unseen=∅]]；存在归因字段时满足第9节归因条件；不存在不可修复的一致性冲突；并且[[MATH:O_r=0]]。任一条件不满足时令[[MATH:StrongPass(a)=0]]。强校验用于阻止多个普通得分抵消一个严重幻觉错误。

输出控制按照可信度区间和强校验结果共同执行。

当[[MATH:Conf(a)≥0.80]]且[[MATH:StrongPass(a)=1]]时，系统输出完整可信解释，包括告警摘要、关键结论、每条结论的证据编号、IOC、满足条件的归因、处置建议、负证据和证据不足说明，并标记为“可信解释”。

当[[MATH:0.60≤Conf(a)<0.80]]，且不存在未见IOC、伪造引用或无法修复的一致性冲突时，系统输出受限解释。系统仅保留[[MATH:Sup(c_j)≥τ_sup]]的结论，删除不满足条件的IOC和归因字段，将确定性用语转换为“可疑”“可能”或“证据不足”，同时输出被删除结论的失败原因，并标记为“受限解释”。

当[[MATH:Conf(a)<0.60]]，或者[[MATH:StrongPass(a)=0]]且失败项属于未见IOC、伪造引用、无支持的高重要等级结论、归因条件不满足、不可修复一致性冲突或风险越界时，系统不输出大模型自由生成的确定性结论，转入第6节的降级证据输出路径，仅输出底层检测结果、可验证证据、负证据、缺失证据类型和校验失败说明，并标记为“仅证据输出”。

实施例中的0.80和0.60通过验证集阈值搜索确定：高阈值优先满足可信解释中未见IOC率和伪造引用率均低于1%，低阈值优先保证被保留关键结论的证据支持率不低于90%。部署环境变化时可在不改变上述三级控制逻辑的前提下更新阈值版本。无论[[MATH:Conf(a)]]数值多高，强失败项均不能通过其他正向分量抵消。

可信度融合与输出控制模块最终形成输出对象：
[[MATH:O_a=(AID,label,R_f,Conf(a),Level,Y_a^*,EID_set,b,O_r,V_m,V_e,V_c,t_out)]]
其中，[[MATH:Level]]为可信解释、受限解释或仅证据输出等级，[[MATH:Y_a^*]]为校验和删除不合格结论后的结构化结果，[[MATH:EID_set]]为实际引用的证据编号集合，[[MATH:b]]为第9节校验失败向量，[[MATH:V_m]]、[[MATH:V_e]]和[[MATH:V_c]]分别为检测模型、证据规则和可信度参数版本，[[MATH:t_out]]为输出时间。该对象与[[MATH:AID]]绑定，使检测结果、证据、生成内容、校验结果和最终输出能够完整回溯，并将聚合后的失败类型和效果指标传递给第11节反馈更新模块。"""


REPLACEMENTS = {
    "M066": SECTION6_TEXT,
    "S16": SECTION9_TEXT,
    "S15": SECTION10_TEXT,
}


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("source", type=Path)
    parser.add_argument("target", type=Path)
    parser.add_argument("report", type=Path)
    args = parser.parse_args()

    specs = json.loads(args.source.read_text(encoding="utf-8"))
    by_id = {item["id"]: item for item in specs}
    expected = DELETE_IDS | set(REPLACEMENTS)
    missing = sorted(expected - set(by_id))
    if missing:
        raise RuntimeError(f"Missing specifications: {missing}")

    for spec_id, replacement in REPLACEMENTS.items():
        if replacement.count("[[MATH:") != len(MATH_RE.findall(replacement)):
            raise RuntimeError(f"Unclosed math marker in {spec_id}.")
        if "]]]" in replacement:
            raise RuntimeError(f"Ambiguous math marker terminator in {spec_id}.")

    before_counts = {
        spec_id: len(MATH_RE.findall(by_id[spec_id]["text"]))
        for spec_id in REPLACEMENTS
    }
    filtered = [item for item in specs if item["id"] not in DELETE_IDS]
    filtered_by_id = {item["id"]: item for item in filtered}
    for spec_id, replacement in REPLACEMENTS.items():
        filtered_by_id[spec_id]["text"] = replacement

    args.target.write_text(
        json.dumps(filtered, ensure_ascii=False, indent=2), encoding="utf-8"
    )
    report = {
        "source_comment_count": len(specs),
        "target_comment_count": len(filtered),
        "retained_replacement_ids": sorted(REPLACEMENTS),
        "deleted_count": len(DELETE_IDS),
        "deleted_ids": sorted(DELETE_IDS),
        "formula_count_before": before_counts,
        "formula_count_after": {
            spec_id: len(MATH_RE.findall(text))
            for spec_id, text in REPLACEMENTS.items()
        },
        "replacement_chars": {
            spec_id: len(text) for spec_id, text in REPLACEMENTS.items()
        },
        "target_formula_count": sum(
            len(MATH_RE.findall(item["text"])) for item in filtered
        ),
    }
    args.report.write_text(
        json.dumps(report, ensure_ascii=False, indent=2), encoding="utf-8"
    )
    print(json.dumps(report, ensure_ascii=False, indent=2))


if __name__ == "__main__":
    main()
