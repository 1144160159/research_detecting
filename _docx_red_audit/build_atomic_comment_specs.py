from __future__ import annotations

import json
from pathlib import Path


OUTPUT = Path(__file__).with_name("output") / "atomic_comment_specs.json"


specs: list[dict[str, object]] = []


def add_marked(
    group: int,
    title: str,
    context: str,
    anchor: str,
    text: str,
    occurrence: int = 1,
    context_occurrence: int = 1,
) -> None:
    specs.append(
        {
            "kind": "marked",
            "group": group,
            "title": title,
            "context": context,
            "anchor": anchor,
            "occurrence": occurrence,
            "context_occurrence": context_occurrence,
            "text": text,
        }
    )


def add_supplement(
    title: str,
    context: str,
    anchor: str,
    text: str,
    occurrence: int = 1,
    context_occurrence: int = 1,
) -> None:
    specs.append(
        {
            "kind": "supplement",
            "title": title,
            "context": context,
            "anchor": anchor,
            "occurrence": occurrence,
            "context_occurrence": context_occurrence,
            "text": text,
        }
    )


# 1. 通信对象术语，5项。
ctx = "请解释一下，TCP连接、UDP或QUIC通信、短连接或高频连接分别指？"
add_marked(1, "TCP连接", ctx, "TCP连接", "TCP连接是指同一方向规范化五元组下，自TCP握手开始，至出现FIN/RST或连续300秒未出现新报文时结束的双向报文集合。300秒为空闲超时实施例，可按链路历史会话间隔的高分位数校准。")
add_marked(1, "UDP通信", ctx, "UDP或QUIC通信", "UDP通信是指同一方向规范化五元组下，相邻数据报间隔不超过30秒的双向数据报集合；超过该空闲超时则建立新会话。30秒为实施例参数。")
add_marked(1, "QUIC通信", ctx, "UDP或QUIC通信", "QUIC通信优先按可见连接标识CID聚合；CID不可见或发生迁移时，以规范化五元组、30秒空闲超时和首部可见连接信息构造近似会话，并记录聚合方式。")
add_marked(1, "短连接", ctx, "短连接或高频连接", "短连接是指持续时间不超过10秒或报文数不超过20个的流对象。阈值为实施例值，可由训练期正常流持续时间和报文数分位点确定并随模型版本保存。")
add_marked(1, "高频连接", ctx, "短连接或高频连接", "高频连接是指同一源端与目的端关系在60秒内建立不少于20个会话。系统采用滑动窗口计数，窗口和次数阈值通过正常业务基线的高分位数校准。")

# 2. 流对象与会话，2项。
ctx = "系统对每个流对象生成唯一流索引"
add_marked(2, "流对象定义", ctx, "流对象指？", "流对象是检测和证据关联的最小处理单元，由会话在固定统计窗口内形成。普通短会话对应一个流对象；持续时间超过统计窗口的长会话被拆分为多个带父会话标识的子流对象。")
add_marked(2, "会话关系", ctx, "与上述得到的会话有何关系吗？", "会话用于描述通信生命周期，流对象用于限定一次特征计算。二者通常一一对应；长会话为一对多关系。流索引可表示为[[MATH:FID=Hash(K_(5tuple)^norm,wid,sid)]]，其中[[MATH:wid]]为窗口编号，[[MATH:sid]]为采集点编号。")

# 3. 协议术语，2项。
ctx = "系统为每个流对象抽取包级特征"
add_marked(3, "协议特征术语关系", ctx, "“协议特征”", "两处表达应统一为“协议可见侧信道特征”，不再分别使用“协议特征”和“协议侧信道特征”，以免形成两个技术概念。")
add_marked(3, "协议侧信道定义", ctx, "如不是，请解释一下协议侧信道特征", "协议可见侧信道特征是指无需解密应用载荷即可从协议首部、握手过程及关联元数据获得的属性，包括TLS/QUIC版本、加密套件数量、SNI、ALPN、证书摘要、JA3/JA4和DNS关联信息。")

# 4. 数值特征，1项。
ctx = "为使不同尺度特征进入统一模型"
add_marked(4, "数值特征范围", ctx, "数值特征", "[[MATH:x_j]]表示包级、流级或协议可见侧信道特征中可直接量化的第[[MATH:j]]个标量。类别字段先采用字典、哈希或嵌入编码；序列字段按位置逐元素缩放并保留掩码，均不直接视为单一标量。", 2)

# 5. 极小常数，1项。
ctx = "防止分母为零的极小常数"
add_marked(5, "极小常数取值", ctx, "可取多少？请举例说明", "极小常数可取[[MATH:epsilon=10^(-8)]]，并可在[[MATH:10^(-12)<=epsilon<=10^(-6)]]内配置。若训练集某维满足[[MATH:x_j^max=x_j^min]]，该维直接置零并记录常量特征标记。")

# 6. 四类输入向量，3项。
ctx = "包级序列特征"
add_marked(6, "包级向量关系", ctx, "与上述包级特征的关系是？", "[[MATH:X_(pkt)]]是包级特征中的序列子集，经前[[MATH:K=16]]包截断、零值补齐、有效位掩码和逐维归一化后形成，不包含持续时间等流级统计量。")
add_marked(6, "流级向量关系", ctx, "与上述流级特征的关系是？", "[[MATH:X_(flow)]]由持续时间、报文数、字节数、上下行比例、包长和间隔统计量组成，是流级特征经缺失值处理与归一化后的定长向量。")
add_marked(6, "四类向量预处理", ctx, "均为归一化后的对应特征吗？", "四类输入均为模型可用表示，但处理方式不同：数值量归一化，类别量编码，序列量截断/补齐并携带掩码，上下文量进行周期编码和历史窗口聚合；不能笼统写成全部执行同一种归一化。")

# 7. 四类检测输出，4项。
ctx = "对于每个流对象"
add_marked(7, "已知类别概率", ctx, "已知类别概率", "轻量分类模型输出logit [[MATH:z_k]]，经Softmax得到[[MATH:p(y=k|f)=e^(z_k)/(sum_(l=1)^C e^(z_l))]]。推理前可在验证集上进行温度缩放，使概率具备可比较的校准含义。", 2)
add_marked(7, "异常分数", ctx, "异常分数", "模型倒数第二层得到流表征[[MATH:h_f]]；第[[MATH:k]]个已知类原型为[[MATH:mu_k=(1/N_k)sum_(i:y_i=k)h_i]]。令[[MATH:d_f=min_k norm(h_f-mu_k)_2]]，异常分数为[[MATH:s_a(f)=min(1,d_f/tau_d)]]，[[MATH:tau_d]]取已知验证样本距离的95%分位数。", 2)
add_marked(7, "规则命中分数", ctx, "规则命中分数", "令[[MATH:I_m(f)in{0,1}]]表示第[[MATH:m]]条规则是否命中，[[MATH:q_m]]为规则可靠度，则[[MATH:s_r(f)=sum_m q_m I_m(f)/(sum_m q_m+epsilon)]]。[[MATH:q_m]]由历史验证样本的精确率确定。", 2)
add_marked(7, "检测不确定性", ctx, "不确定性", "采用归一化熵[[MATH:U_f=-sum_(k=1)^C p_k log(p_k+epsilon)/log C]]，其中[[MATH:C]]为已知类别数，故[[MATH:0<=U_f<=1]]。正常类若参与分类，应计入[[MATH:C]]并在类别表中明确。", 2)

# 8. 风险融合参数和阈值，7项。
ctx = "为Sigmoid函数"
add_marked(8, "融合权重", ctx, "权重如何得到，或者可取值多少？", "[[MATH:w_1]]至[[MATH:w_4]]通过仅含训练集和验证集的非负约束逻辑回归确定。实施例可取[[MATH:(w_1,w_2,w_3,w_4)=(1.0,0.8,0.6,0.7)]]，并与模型版本、阈值版本同时保存。")
add_marked(8, "偏置项", ctx, "如何得到，或可取多少？", "偏置[[MATH:b]]与融合权重联合训练，用于校准基础恶意率。实施例可取[[MATH:b=-1.2]]；不得在测试集上重新拟合。")
add_marked(8, "恶意阈值", ctx, "恶意阈值可取多少？请举例说明", "恶意阈值记为[[MATH:tau_m]]，实施例取[[MATH:tau_m=0.70]]。其由验证集在误报率约束下最大化Macro-F1或Youden指数确定。")
add_marked(8, "中间区间", ctx, "中间区间为哪个区间？", "中间风险区间可设置为[[MATH:0.35<=R_f<0.70]]；下界为正常阈值[[MATH:tau_n=0.35]]，上界为恶意阈值[[MATH:tau_m=0.70]]。")
add_marked(8, "高不确定性", ctx, "如何判断“不确定较高”，标准是？", "当[[MATH:U_f>=tau_u^H]]时判定为高不确定性，实施例取[[MATH:tau_u^H=0.45]]。阈值由已知验证集熵分布和未知代理验证集共同校准。")
add_marked(8, "低风险", ctx, "如何判断“风险较低”", "当[[MATH:R_f<tau_n=0.35]]时判定风险较低。若规则存在强制恶意命中，则先执行硬规则，不由低融合风险覆盖。")
add_marked(8, "低不确定性", ctx, "“不确定较低”，标准分别是？", "当[[MATH:U_f<tau_u^L]]时判定为低不确定性，实施例取[[MATH:tau_u^L=0.30]]。介于0.30和0.45之间时保留中等不确定状态，避免高低阈值相同导致抖动。")

# 9. 检测模块及模型输出，3项。
ctx = "检测模块输出的不仅是标签和分数"
add_marked(9, "检测模块指代", ctx, "检测模块指？", "本段检测模块专指由规则初筛、轻量分类、原型距离异常检测和风险校准组成的恶意流量检测模块，不包括后续大模型生成与幻觉校验模块。")
add_marked(9, "常规技术与组合创新", ctx, "为现有常规技术吗？", "特征贡献计算、规则记录、版本记录和上下文记录可分别采用现有实现；拟保护点在于以统一流索引把四类结果组织为可追溯检测证据，并作为证据包、受控生成和幻觉校验的共同输入。")
add_marked(9, "模型输出指代", ctx, "模型输出指？具体指哪个模型？", "“模型输出”应限定为轻量恶意流量分类模型和原型距离异常检测分支的输出；规则命中属于规则分支记录，不应混称为大模型输出。")

# 10. 特征贡献集合，2项。
ctx = "为特征贡献集合"
add_marked(10, "贡献集合计算", ctx, "特征贡献集合如何得到", "树模型采用TreeSHAP，线性模型采用特征值与系数乘积，浅层神经网络采用梯度乘输入，得到[[MATH:contrib_j]]。按绝对贡献排序并保留前[[MATH:M=10]]项构成[[MATH:C_f]]。")
add_marked(10, "高贡献项关系", ctx, "与贡献较高的特征项的关系是？", "“贡献较高的特征项”就是[[MATH:C_f]]中按[[MATH:abs(contrib_j)]]排序靠前的元素。相关系数绝对值超过0.95的特征只保留贡献绝对值较大者，并保留正负方向。")

# 11. 风险等级映射，3项。
ctx = "不同检测器分数区间不一致"
add_marked(11, "现有技术说明", ctx, "该过程是否为现有技术？", "线性归一化、Platt缩放和等距回归本身可作为现有校准手段；本方案利用校准结果形成统一告警对象，并将校准版本与证据包绑定。")
add_marked(11, "等级映射表生成", ctx, "等级映射表如何得到", "等级映射表由历史验证样本中各等级对应的实际恶意比例估计；样本不足时可用等级区间中点初始化。示例为正常0.10、低0.30、中0.50、高0.75、严重0.95。")
add_marked(11, "等级转数值风险", ctx, "如何根据等级映射表转换为数值风险", "读取检测器标识和校准版本，以原始等级为键查询映射值，得到[[MATH:r_a in[0,1]]]；未知等级进入缺失处理，不得默认映射为高风险。")

# 12. 五类证据来源、取得和统一转换，11项。
ctx = "检测证据抽取与证据向量化模块用于围绕标准化告警"
evidence_specs = [
    ("流量证据", "流记录库、包序列摘要库和协议可见字段索引", "按[[MATH:FID]]及告警前后各60秒窗口读取流统计、前16包序列和协议摘要；缺失字段写入missing_mask。"),
    ("模型证据", "检测结果记录库和模型运行日志", "按[[MATH:FID]]、detector_id和run_id读取类别概率、异常分数、规则分数、不确定性、阈值版本及贡献项。"),
    ("资产证据", "带生效时间的资产与业务基线表", "按源/目的IP、端口和告警时间匹配资产类型、业务角色、常用端口和服务暴露状态；无法匹配时生成“资产未知”证据。"),
    ("历史证据", "历史告警库、相似流向量索引和已确认事件库", "仅检索告警时间之前30天内相同源、目的、指纹或表征相似度不低于0.85的事件，最多保留20条并按AID去重。"),
    ("负证据", "正常业务基线、白名单、历史误报记录和字段一致性规则库", "将当前攻击假设与基线及冲突规则比对，记录反驳的claim_type、冲突字段、规则编号和原始索引；过期白名单不得使用。"),
]
for name, location, acquisition in evidence_specs:
    add_marked(12, f"{name}存储位置", ctx, name, f"{name}存储于{location}。证据对象必须保存来源系统、存储索引和有效时间，保证后续能够回溯。")
    add_marked(12, f"{name}取得过程", ctx, name, acquisition)
add_marked(12, "统一证据对象", ctx, "如何转换为统一的证据对象？", "各来源记录先完成字段映射、类型转换、时间统一、去重和缺失标记，再生成[[MATH:E_i={eid,type,value,source,t_start,t_end,quality,AID,FID,raw_ref,version}]]。证据编号由AID、类型、序号和内容摘要联合生成。")

# 13. 证据向量六维量，6项。使用完整句定位，避免落到上一段同名问句。
ctx = "来源类型编码"
add_marked(13, "相关度", ctx, "相关度", "定义[[MATH:rel_i=0.30M_field+0.20M_time+0.20M_label+0.10M_contrib+0.20M_sem]]。各分量归一化至0至1；无文本时删除语义项并重新归一化其余权重。", context_occurrence=2)
add_marked(13, "来源可信度", ctx, "来源可信度", "定义[[MATH:trust_i=r_(src,i)c_i]]。[[MATH:r_(src,i)]]来自版本化来源可靠度表，[[MATH:c_i=0.5c_field+0.3c_packet+0.2c_trace]]表示字段完整率、报文完整率和可回溯性。", context_occurrence=2)
add_marked(13, "新鲜度", ctx, "新鲜度", "定义[[MATH:fresh_i=e^(-abs(t_i-t_a)/tau_type)]]。实施例中流量/模型证据[[MATH:tau=3600s]]、资产证据7天、历史证据30天。", context_occurrence=2)
add_marked(13, "稀有度", ctx, "稀有度", "在告警前历史窗口统计证据值频率，采用拉普拉斯平滑[[MATH:P_i=(n_i+1)/(N+B)]]，再计算[[MATH:rare_i=min(1,-log(P_i+epsilon)/kappa)]]。", context_occurrence=2)
add_marked(13, "负证据强度", ctx, "负证据强度", "定义[[MATH:neg_i=clip(0.4I_field+0.3I_base+0.3P_contra,0,1)]]，分别表示字段矛盾、业务基线冲突和固定蕴含模型的矛盾概率。", context_occurrence=2)
add_marked(13, "来源类型编码", ctx, "来源类型编码", "来源划分为流量、检测模型、规则、资产、历史事件、业务基线和外部摘要七类，[[MATH:src_i]]采用7维独热编码；未知来源使用全零向量并设置unknown_source标记。", context_occurrence=2)

# 14. 证据排序与质量量，12项。
ctx = "分别如何得到或取值？"
add_marked(14, "冗余度", ctx, "如何得到？", "定义[[MATH:red_i=max_(e_j in S)cos(v_i,v_j)]]。当已选集合[[MATH:S]]为空时令[[MATH:red_i=0]]；相似度先映射至0到1并按固定编码模型版本计算。")
weight_info = [
    ("alpha", "相关度权重", "0.30"),
    ("beta", "来源可信度权重", "0.20"),
    ("gamma", "新鲜度权重", "0.15"),
    ("delta", "稀有度权重", "0.10"),
    ("mu", "负证据强度权重", "0.15"),
    ("nu", "冗余惩罚权重", "0.10"),
]
for symbol, meaning, value in weight_info:
    add_marked(14, meaning, ctx, "分别如何得到或取值？", f"[[MATH:{symbol}]]表示{meaning}，实施例取[[MATH:{symbol}={value}]]。六个参数通过验证集联合搜索，使引用覆盖率提高、无证据结论率降低且上下文长度受控。")
add_marked(14, "相关度计算过程", ctx, "相关度", "相关度按字段一致率、时间重叠率、标签规则匹配、特征贡献命中和语义相似度五项加权计算；每项均保留原始分量，便于复算。", 2)
add_marked(14, "可信度数值", ctx, "如何得到具体数值？", "来源可信度使用[[MATH:trust_i=r_(src,i)c_i]]；基础可靠度示例为原始流摘要0.95、模型记录0.90、资产基线0.80、已确认历史事件0.85、外部摘要0.50。", 1)
add_marked(14, "新鲜度数值", ctx, "如何得到具体数值？", "新鲜度按[[MATH:fresh_i=e^(-abs(t_i-t_a)/tau_type)]]计算；时间晚于告警且不属于补证结果的记录标记为时间越界。", 2)
add_marked(14, "稀有度数值", ctx, "如何得到具体数值？", "稀有度依据告警前基准窗口的平滑频率计算；连续值先按训练集分位点离散化，避免每个浮点数均被误判为稀有。", 3)
add_marked(14, "负证据强度计算", ctx, "如何得到？，请进一步说明", "负证据强度由字段冲突、基线冲突和固定版本矛盾模型概率加权得到，并以[[MATH:neg_i]]进入排序及负证据压力；不得由大模型自报。")

# 15. 余弦相似度，2项。
ctx = "对于文本或摘要证据"
add_marked(15, "相似度所属参数", ctx, "得到的相关性为上述哪个参数？", "余弦相似度对应[[MATH:rel_i]]中的语义匹配分量[[MATH:M_sem]]，不是新增独立参数。")
add_marked(15, "相似度使用方法", ctx, "或者如何使用？", "告警查询文本和证据摘要由同一固定版本编码模型生成向量，计算[[MATH:M_sem=(1+cos(q,e_i))/2]]后进入相关度公式；模型更新时必须重建证据向量索引。")

# 16. 覆盖率阈值，1项。
ctx = "若证据覆盖率过低"
add_marked(16, "覆盖率过低标准", ctx, "如何判定覆盖率过低，比如低于多少？", "实施例设置[[MATH:theta_cov=0.60]]：[[MATH:Cov(a)<0.60]]为低覆盖，0.60至0.80为中等覆盖，不低于0.80为充分覆盖。必要类型权重和最低数量随告警类型配置。")

# 17. 路径调度输入和五条路径，7项。
ctx = "负证据压力如何得到？"
add_marked(17, "不确定性指代", ctx, "即", "是。此处统一记为归一化检测不确定性[[MATH:U_f]]，全文不再混用[[MATH:u(f)]]和[[MATH:U_f]]。")
add_marked(17, "负证据压力", ctx, "负证据压力如何得到？", "定义[[MATH:P_neg=sum_(e_i in E_neg)q_i neg_i/(sum_(e_i in E_neg)q_i+epsilon)]]，其中[[MATH:q_i]]为非负归一化证据权重；无负证据时令[[MATH:P_neg=0]]。")
path_comments = {
    "轻量模板路径": "输入基础告警和已有证据编号；当风险低、覆盖不足且无敏感实体时触发；使用固定模板输出摘要、证据编号和缺口，不调用大模型；字段缺失则转降级输出。",
    "完整生成路径": "输入覆盖充分的当前版本证据包；执行证据约束提示、大模型结构化生成、命题化和全部校验；输出通过校验的可信或降级解释；超时转降级输出。",
    "补证路径": "输入缺失证据类型列表；按AID、FID和时间窗检索可补证来源，生成新证据包版本并重算覆盖率；达到阈值后重新选路，未达到则转轻量或降级路径。",
    "高强度校验路径": "输入含IOC、归因或高风险命题的结构化初稿；除基础校验外执行IOC规范化、归因证据门限、语义支持和风险越界校验；失败时最多重写一次，之后降级。",
    "降级输出路径": "输入校验失败命题、现有证据和缺口列表；删除不受支持的IOC、归因和确定性表述，仅输出已知事实、有效证据编号及待补项；不得再次生成新事实。",
}
for name, procedure in path_comments.items():
    add_marked(17, name, ctx, "各路径", procedure)

# 18. 路径目标函数，9项。
ctx = "分别可取多少，请举例说明"
add_marked(18, "路径收益G", ctx, "如何得到？", "定义[[MATH:G(a,p)=0.4DeltaCov(a,p)+0.3PassHist(type,p)+0.3R_a I_high]]，三项均归一化至0至1并只使用告警发生前统计。", 1)
add_marked(18, "路径成本C", ctx, "如何得到？", "定义[[MATH:C(a,p)=0.4Tok/B_tok+0.4Lat/B_lat+0.2Call/B_call]]并截断至0至1。预算实施例为4096 Token、3000毫秒和2次模型调用。", 2)
add_marked(18, "幻觉风险H", ctx, "如何得到？", "定义[[MATH:H(a,p)=clip(0.30U_f+0.25(1-Cov)+0.20P_neg+0.15I_IOC+0.10I_attr,0,1)]]；高强度校验路径仅可按历史通过率折扣，不得置零。", 3)
add_marked(18, "证据缺口D", ctx, "如何得到？", "定义[[MATH:D(a,p)=sum_(t in T)w_t max(0,m_t-nhat_t(a,p))/sum_(t in T)w_t]]，[[MATH:nhat_t]]为执行路径后预计证据数。", 4)
add_marked(18, "成本权重lambda", ctx, "分别可取多少，请举例说明", "[[MATH:lambda]]为路径成本权重，实施例取[[MATH:lambda=0.25]]；通过验证集时延和Token预算约束校准。")
add_marked(18, "幻觉权重rho", ctx, "分别可取多少，请举例说明", "[[MATH:rho]]为潜在幻觉风险权重，实施例取[[MATH:rho=0.45]]；对IOC和归因敏感任务可提高。")
add_marked(18, "缺口权重eta", ctx, "分别可取多少，请举例说明", "[[MATH:eta]]为证据缺口惩罚权重，实施例取[[MATH:eta=0.30]]；其取值与必要证据类型配置共同保存。")
add_marked(18, "最优路径p星", ctx, "表示？", "[[MATH:p^*]]表示在满足硬约束的可行路径集合中，使[[MATH:G-lambda C-rho H-eta D]]最大的最终执行路径。", 1)
add_marked(18, "候选路径集合P", ctx, "表示？", "[[MATH:P]]表示五条候选处理路径组成的集合；经预算、字段完整性和强制校验要求筛选后得到[[MATH:P_feasible subseteq P]]。", 2)

# 19. 统一选路逻辑，3项。
ctx = "低风险、低覆盖率或字段不完整的告警进入轻量模板路径"
add_marked(19, "公式与文字关系", ctx, "还是根据此段话进行路径选择", "采用“硬约束筛选+目标函数排序”。本段文字条件用于构造[[MATH:P_feasible]]，不是第二套独立选路规则；最终执行路径统一由[[MATH:p^*]]给出。")
add_marked(19, "路径对应判断", ctx, "如何判断根据", "系统为五条路径分别计算效用值，并保留path_id；[[MATH:p^*=argmax_(p in P_feasible)U(a,p)]]返回的path_id直接对应具体路径。并列时优先选择成本较低且校验强度不降低的路径。")
add_marked(19, "p星使用", ctx, "则计算得到", "[[MATH:p^*]]用于调用对应路径执行器、记录调度日志并更新历史通过率；不得只计算而仍由固定文字规则覆盖其结果。")

# 20. 命题风险和待校验字段，3项。
ctx = "风险等级如何得到？"
add_marked(20, "生成风险状态", ctx, "风险等级如何得到？", "建议统一称“生成风险状态”。大模型仅能输出正常、可疑、高风险、已确认四个枚举值，并分别映射为[[MATH:0.10,0.40,0.70,0.90]]；后续风险越界校验决定是否保留。")
add_marked(20, "待校验字段定义", ctx, "待校验字段是指？", "待校验字段是命题对象中必须经过机器校验的结构化字段，包括evidence_ids、risk_status、entities、ioc_list、attribution和action，不是任意自然语言片段。")
add_marked(20, "待校验字段生成", ctx, "如何得到？", "模型按JSON Schema输出claim数组；系统依据claim_type选择必校验字段，并通过实体抽取器补充IOC和归因实体。字段缺失即标记结构不完整并进入一次受约束修复。", 2)

# 21. 证据编号和关键结论，2项。
ctx = "结构校验用于检查模型输出"
add_marked(21, "证据编号来源", ctx, "证据编号如何得到？", "证据编号在构包阶段生成，可表示为[[MATH:eid=Hash(AID,type,seq,content_digest,version)]]。大模型输出必须包含evidence_ids且只能引用当前证据包版本的编号。")
add_marked(21, "关键结论来源", ctx, "关键结论未包括在大模型输出中，如何得到？", "大模型结构化输出中增加key_conclusions数组，每项包含claim_id、claim_text、claim_type、risk_status、evidence_ids和insufficient_evidence；校验模块不再从自由文本猜测关键结论。")

# 22. 命题支持度，7项。
ctx = "为与命题冲突的负证据集合"
add_marked(22, "引用证据范围", ctx, "包括检测证据集合、流量证据集合、环境证据集合吗？", "[[MATH:Ref(c_j)]]仅包含命题显式引用、编号有效且存在于当前证据包中的检测、流量、环境、历史或负证据。不同claim_type使用不同允许来源表。")
add_marked(22, "负证据子集", "分别表示？，如何取值？", "即", "[[MATH:Neg(c_j)]]是与命题[[MATH:c_j]]相关并发生冲突的负证据子集，满足[[MATH:Neg(c_j) subseteq E_neg]]，不等于证据包中的全部负证据。")
add_marked(22, "支持匹配度", ctx, "具体如何计算？", "定义[[MATH:match(c_j,e_i)=clip(0.4M_rule+0.25M_sem+0.35P_entail,0,1)]]。无可比文本时删除语义和蕴含项并重新归一化。", 1)
add_marked(22, "冲突程度", ctx, "具体如何计算？", "定义[[MATH:conflict(c_j,e_k)=clip(0.5I_rule+0.5P_contra,0,1)]]，规则项检查风险状态、IOC归属、资产角色和时间关系，并保存触发规则编号。", 2)
add_marked(22, "正证据权重", ctx, "分别表示？", "[[MATH:omega_i]]表示支持证据[[MATH:e_i]]的归一化权重，可取[[MATH:omega_i=Score^+(e_i)/(sum_h Score^+(e_h)+epsilon)]]。", 1)
add_marked(22, "负证据权重", ctx, "分别表示？", "[[MATH:omega_k]]表示冲突负证据[[MATH:e_k]]的归一化权重，可取[[MATH:omega_k=Score^+(e_k)/(sum_h Score^+(e_h)+epsilon)]]。", 1)
add_marked(22, "支持度权重取值", ctx, "如何取值？", "正、负证据均先按非负得分归一化；负证据总惩罚系数可取[[MATH:kappa=0.6]]。集合为空时对应加权和置零，避免分母为零。")

# 23. 支持度阈值，2项。
ctx = "则该命题不得作为确定结论输出"
add_marked(23, "支持度阈值含义", ctx, "表示？", "[[MATH:theta_s]]表示命题能够作为确定性结论输出所需的最低支持度。不同命题类型可配置不同阈值并与校验策略版本绑定。")
add_marked(23, "支持度阈值取得", ctx, "如何得到，或取值？", "实施例取[[MATH:theta_s=0.65]]。在验证集上以控制无证据结论率为约束，最大化有效结论保留率；低于阈值的命题删除或改写为证据不足。")

# 24. IOC集合来源，3项。
ctx = "未见IOC校验用于检查模型"
add_marked(24, "输出指代", ctx, "输出指大模型输出吗", "是，输出专指大模型生成的结构化解释初稿，不包括底层检测器输出或最终可信输出。")
add_marked(24, "IOCout抽取字段", ctx, "具体从哪个输出中抽取候选IOC集合？", "从结构化初稿的summary、key_conclusions、ioc_list和attribution字段抽取；IP、域名、URL、哈希、证书指纹和账号标识分别采用确定性解析器。")
add_marked(24, "可见IOC集合", ctx, "上述证据包中未出现可见IOC集合？", "证据包对象需新增visible_ioc字段，汇总检测、流量、环境和历史证据中可追溯的IOC，形成[[MATH:IOC_ep]]；每个IOC同时保存来源证据编号。")

# 25. 新IOC集合，1项。
ctx = "则对应结论被标记为未见IOC风险"
add_marked(25, "未见IOC集合", ctx, "表示？", "[[MATH:IOC_new=IOC_out setminus IOC_ep]]，表示生成初稿中出现但当前证据包不可见的IOC。集合非空时对应命题强校验失败，删除后重写或直接降级。")

# 26. 归因校验时点，1项。
ctx = "归因校验用于检查模型"
add_marked(26, "归因校验时点", ctx, "攻击组织、攻击家族或特定工具是何时执行的？", "这里不是“执行攻击组织或工具”，而是在大模型初稿生成并命题化后，从entities或attribution字段识别组织、家族和工具名称，再执行归因证据校验。")

# 27. 风险术语，1项。
ctx = "风险越界校验用于判断"
add_marked(27, "风险术语统一", ctx, "风险等级是指模型输出的风险状态吗？", "是。生成侧统一使用“生成风险状态”，检测侧使用连续风险分数[[MATH:R_f]]。生成风险状态映射为[[MATH:R_gen]]后只用于越界比较，全文删除混用的“风险等级”表述。")

# 28. 生成风险值和偏差，2项。
ctx = "为生成文本对应的风险等级数值"
add_marked(28, "生成风险值", ctx, "如何得到？", "[[MATH:R_gen]]由结构化输出risk_status映射得到：正常0.10、可疑0.40、高风险0.70、已确认0.90；不得从自由文本主观估值。")
add_marked(28, "允许解释偏差", ctx, "可取多少？", "允许偏差实施例取[[MATH:Delta=0.15]]，并可在[[MATH:0.10<=Delta<=0.20]]内按验证集风险越界误放率校准。")

# 29. 风险越界结果，1项。
ctx = "后，如何使用？"
add_marked(29, "风险越界用途", ctx, "后，如何使用？", "若[[MATH:F_risk>0]]，对应风险命题先执行硬阻断并进入降级或重写；同时将[[MATH:F_risk]]作为最终可信度的连续扣分项，形成硬阻断与软惩罚两级控制。")

# 30. 可信度变量与输出控制，9项。
ctx = "若可信度高于输出阈值"
add_marked(30, "检测风险指代", ctx, "与此处的检测风险是一个吗？", "是，统一称融合检测风险[[MATH:R_f]]；标准化告警风险[[MATH:r_a]]在本发明检测器场景下等于[[MATH:R_f]]，外部检测器场景下由校准得到。")
add_marked(30, "平均支持度", ctx, "的平均值吗？", "是，但应采用命题重要度加权平均[[MATH:Support_bar=sum_j v_j Support(c_j)/(sum_j v_j+epsilon)]]，而非简单算术平均。")
add_marked(30, "引用质量", ctx, "证据引用质量（如何得到？）", "定义[[MATH:Q_ref=N_valid/N_required]]，其中有效引用需同时满足编号存在、版本一致、类型允许和可回溯；无关键命题时令[[MATH:Q_ref=0]]。")
add_marked(30, "检测不确定性指代", "证据引用质量", "模型不确定性", "是，统一为归一化检测不确定性[[MATH:U_f]]；不得再引入含义不同但同名的生成不确定性。")
add_marked(30, "幻觉失败惩罚", ctx, "幻觉校验失败惩罚（如何得到？）", "定义[[MATH:F_hall=sum_l h_l I_l/(sum_l h_l+epsilon)]]。权重示例：结构0.10、引用0.15、支持度0.15、未见IOC 0.20、归因0.15、一致性0.10、风险越界0.15。")
add_marked(30, "可信输出阈值", ctx, "输出阈值可取多少，请举例说明", "可信输出阈值实施例取[[MATH:tau_T^H=0.75]]，并要求全部强校验通过；阈值在验证集上按错误确定性结论率约束校准。")
add_marked(30, "强校验定义", ctx, "强校验通过是指？", "强校验通过是指结构完整、引用编号有效、未见IOC集合为空、归因证据达到门限且无风险越界。任一失败均不得被其他高分抵消。")
add_marked(30, "可信度中间区间", ctx, "中间区间具体指哪个区间？", "实施例中间区间为[[MATH:0.50<=T(a)<0.75]]，输出降级解释并突出证据缺口。")
add_marked(30, "可信度低阈值", ctx, "该阈值可取值多少？请举例说明", "低阈值实施例取[[MATH:tau_T^L=0.50]]；[[MATH:T(a)<0.50]]时只输出已有事实、有效证据编号和待补证项。")

# 31. 七个可信度权重的含义与取值，14项。
ctx = "分别表示？；如何取值？"
theta_info = [
    (1, "检测校准置信度[[MATH:Conf_det=1-U_f]]的权重", "0.20"),
    (2, "证据覆盖率[[MATH:Cov(a)]]的权重", "0.20"),
    (3, "平均命题支持度[[MATH:Support_bar]]的权重", "0.25"),
    (4, "引用质量[[MATH:Q_ref]]的权重", "0.15"),
    (5, "检测不确定性惩罚[[MATH:U_f]]的权重", "0.08"),
    (6, "幻觉失败惩罚[[MATH:F_hall]]的权重", "0.07"),
    (7, "风险越界惩罚[[MATH:F_risk]]的权重", "0.05"),
]
for idx, meaning, value in theta_info:
    add_marked(31, f"theta{idx}含义", ctx, "分别表示？；如何取值？", f"[[MATH:theta_{idx}]]表示{meaning}。风险严重度本身不作为解释可信度正向项。")
    add_marked(31, f"theta{idx}取值", ctx, "分别表示？；如何取值？", f"实施例取[[MATH:theta_{idx}={value}]]。七个权重满足非负且和为1，通过验证集上的错误确定性结论率、有效结论保留率和校准误差联合优化。")

# 32. Q学习变量，2项。锚点包含对应变量，避免落到前文路径公式的“表示”。
ctx = "表示？："
add_marked(32, "Q值", ctx, "表示？", "[[MATH:Q(s_t,a_t)]]表示在状态[[MATH:s_t]]选择路径动作[[MATH:a_t]]后可获得的期望累计折扣收益。状态由风险、覆盖率、不确定性、负证据压力和预算分箱组成。", 1)
add_marked(32, "下一状态最大价值", ctx, "表示？", "原式应写为[[MATH:max_(a in A_feasible(s_(t+1)))Q(s_(t+1),a)]]，表示下一状态可行动作集合中的最大后续价值；未通过硬约束的动作不参与最大化。", 2)

# 33. 图3三种方案，3项。
ctx = "图3中，直接生成是指"
add_marked(33, "直接生成", ctx, "直接生成是指？", "直接生成是指只向大模型输入基础告警字段，不检索证据、不构造证据编号、不执行逐命题幻觉校验的基线方案。")
add_marked(33, "检索生成", ctx, "检索生成是指？", "检索生成是指检索Top-K相关证据后生成解释，但不构建分层证据包、不强制逐结论引用，也不执行未见IOC、归因和风险越界等多级校验。")
add_marked(33, "与本发明区别", ctx, "与本发明方法的区别在于哪？", "本发明在共同底层检测器条件下增加标准化告警、结构化证据包、证据约束生成、逐命题校验、可信度控制和受控反馈；因此应比较解释正确率等生成侧指标。")

# 34. 六个数据集、证据数量和数据规模，18项。
ctx = "其中数据集1至6"
dataset_candidates = [
    "USTC-TFC2016",
    "CIC-IDS2017",
    "CSE-CIC-IDS2018",
    "CIC-DDoS2019",
    "CIC-Darknet2020",
    "TON_IoT",
]
for idx, name in enumerate(dataset_candidates, start=1):
    add_marked(34, f"数据集{idx}", ctx, "数据集1至6", f"候选实验设计中，数据集{idx}可设置为{name}。该名称必须与实际完成实验的日志、划分和结果文件核对；未实测时只能标记为待补，不得写成已完成效果。")
for idx, count in enumerate([4, 8, 12, 16, 24, 32], start=1):
    add_marked(34, f"证据数量{idx}", ctx, "证据数量1至6", f"证据数量{idx}的设计值可取每告警{count}条，用于上下文预算消融。该值表示选入证据包的上限，不是原始库中证据总量，需由实际实验记录确认。")
for idx, scale in enumerate([1, 5, 10, 20, 50, 100], start=1):
    add_marked(34, f"数据规模{idx}", ctx, "数据规模1至6", f"数据规模{idx}的设计值可取{scale}万条流对象或告警。必须统一统计口径并报告类别构成、划分、重复次数、均值和标准差；无实际日志时标记为待补。")

# 35. 图1，1项。
ctx = "图1字太小了"
add_marked(35, "图1清晰度处理", ctx, "图1字太小了，不清楚", "已按4K重新绘制图1并统一使用黑体，删除省略号和重复细粒度框；网络流量作为恶意流量检测输入，主机/设备/应用日志等移至环境证据支路，保留检测、构包、生成、校验、输出和反馈主链。")


# 未标红但影响公开充分性和一致性的20项。
add_supplement("会话键重复", "以五元组、协议类型和时间窗口为基本键构造会话", "五元组、协议类型", "五元组通常已经包含协议号。建议改为“方向规范化五元组与时间窗口”，避免协议类型重复计入会话键。")
add_supplement("会话超时与统计窗口", "对于UDP或QUIC通信", "对于UDP或QUIC通信", "会话空闲超时用于判定通信结束，统计窗口用于切分长会话，两者应分别定义。实施例：TCP 300秒、UDP/QUIC 30秒；统计窗口60秒、步长30秒。")
add_supplement("归一化无泄漏", "为使不同尺度特征进入统一模型", "特征归一化处理", "最大值和最小值只能由训练集或告警发生前历史基准窗口估计，推理阶段固定，禁止使用包含测试样本的全量数据重新计算。")
add_supplement("熵归一化", "模型不确定性可采用熵形式表示", "模型不确定性可采用熵形式表示", "正文熵公式应除以[[MATH:log C]]，保证[[MATH:U_f in[0,1]]]并与0.30、0.45等阈值一致。")
add_supplement("低置信重复计权", "融合风险可表示为", "融合风险可表示为", "[[MATH:1-max_k p_k]]与熵不确定性均反映低置信，建议做消融；相关系数超过0.8时删除其一或约束二者权重和，避免重复放大。")
add_supplement("风险变量统一", "生成统一风险值", "生成统一风险值", "规定本发明检测器输出时[[MATH:r_a=R_f]]；外部检测器先校准得到[[MATH:r_a]]。证据包及路径调度统一读取标准化风险，不能混用。")
add_supplement("证据得分范围", "系统首先为每条证据计算综合得分", "系统首先为每条证据计算综合得分", "线性证据得分可能为负或超过1。建议排序后采用[[MATH:Score_i^+=max(0,Score_i)]]并归一化，后续不得把原始排序分直接当作概率。")
add_supplement("覆盖率边界", "证据覆盖率用于衡量当前证据包", "证据覆盖率用于衡量当前证据包", "若必要类型集合为空或权重和为零，应拒绝计算并使用告警类型默认配置；重复证据只计一次，避免人为提高覆盖率。")
add_supplement("路径量纲统一", "路径选择可表示为", "路径选择可表示为", "[[MATH:G,C,H,D]]相减前必须统一归一化至0至1，并记录统计窗口；不可行路径效用设为负无穷，不能由高收益抵消硬约束。")
add_supplement("路径失败分支", "如图2所示，本发明还提供一种", "如图2所示，本发明还提供一种", "图2中补证、降级和重写应改为条件分支：缺证进入补证，支持度或预算不足进入降级，可修复结构/引用错误进入一次重写，重写失败后降级。")
add_supplement("命题解析失败", "生成完成后，系统把模型输出拆解为关键结论命题", "生成完成后，系统把模型输出拆解为关键结论命题", "优先要求模型按JSON Schema输出。解析失败只允许一次受约束修复；仍失败则进入降级输出，不从任意长文本反复猜测字段。")
add_supplement("IOC规范化", "未见IOC校验用于检查模型", "未见IOC校验用于检查模型", "比较前应规范化：IP去除前导零并统一IPv6压缩形式，域名转小写并去尾点，URL规范化方案/主机，哈希转小写，证书指纹去分隔符。")
add_supplement("归因充分门限", "归因校验用于检查模型", "归因校验用于检查模型", "确定归因至少要求两类独立证据且加权支持度不低于0.80，并包含可回溯规则、家族特征或历史确认记录；否则只能输出候选归因。")
add_supplement("风险不等于可信", "可信度可表示为", "可信度可表示为", "恶意风险越高不代表解释越可信。建议把正向[[MATH:R_f]]替换为[[MATH:Conf_det=1-U_f]]或检测器一致性，[[MATH:R_f]]仅用于路径优先级和越界比较。")
add_supplement("充分性重复计权", "可信度融合与输出控制模块用于将检测风险", "可信度融合与输出控制模块用于将检测风险", "[[MATH:Cov]]、平均支持度和[[MATH:Q_ref]]可能高度相关。应在验证集检查相关矩阵和方差膨胀因子，必要时合并为证据质量子分数。")
add_supplement("强校验先行", "该模块是本发明保证可信输出的关键环节", "该模块是本发明保证可信输出的关键环节", "结构缺失、无效引用、未见IOC、无证据归因和风险越界先执行硬阻断；连续[[MATH:F_hall]]仅用于可降级问题，不能抵消硬失败。")
add_supplement("反馈自强化控制", "系统根据运行结果更新检测阈值", "系统根据运行结果更新检测阈值", "仅使用具有后续确认标签且通过全部强校验的样本更新参数；新参数先在影子版本评估，达到固定验证集门限后发布并保留回滚版本。")
add_supplement("强化学习完整性", "在需要自适应调度时", "在需要自适应调度时", "补充奖励[[MATH:r_t=c_1Y_trusted+c_2Gain_evid-c_3F_hall-c_4Lat-c_5Cost]]、学习率0.1、折扣0.9、初始探索率0.1、离线训练、终止条件和性能下降回滚门限。")
add_supplement("图3指标因果", "恶意流量识别率、证据引用覆盖率、无证据结论比例", "恶意流量识别率", "若三种生成方案共用同一底层检测器，生成方式不会直接改变恶意流量识别率。该指标应改为告警解释正确率或关键结论支持率，识别率另做反馈开关消融。")
add_supplement("实施例与图对应", "实施例一，面向加密恶意流量的检测与解释", "实施例一，面向加密恶意流量的检测与解释", "三个实施例均需给出硬件、模型版本、样本量、类别构成、划分、证据数量、阈值、基线、重复次数、均值和标准差；没有日志时不得写成实测结论。")
add_supplement("证据总体存储位置", "这些证据存在于哪里？，分别如何得到？", "这些证据存在于哪里？", "五类证据不集中存放于同一文件：流量证据位于流记录库、包序列摘要库和协议可见字段索引；模型证据位于检测结果记录库和模型运行日志；资产证据位于版本化资产与业务基线库；历史证据位于历史告警库、相似流索引和已确认事件库；负证据位于正常业务基线、白名单、历史误报记录和一致性规则库。统一证据对象保存[[MATH:source_uri]]、[[MATH:source_version]]、[[MATH:raw_ref]]和[[MATH:time_window]]等定位字段，并通过[[MATH:AID]]与[[MATH:FID]]关联。")
add_supplement("五类证据取得总流程", "这些证据存在于哪里？，分别如何得到？", "分别如何得到？", "系统以标准化告警中的[[MATH:AID]]、[[MATH:FID]]、源/目的标识和告警时间为统一检索条件：先按[[MATH:FID]]取得流量与模型记录，再按通信实体和生效时间匹配资产基线，随后在告警发生前的历史窗口检索相似事件，最后将当前攻击假设与正常基线、白名单和一致性规则比对得到负证据。各结果完成时间统一、去重、缺失标记和来源版本绑定后进入证据对象转换。")
add_supplement("路径共同执行过程", "各路径分别如何执行？，过程为？", "各路径分别如何执行？，过程为？", "五条路径均按输入校验、可行性判定、路径动作、结果校验和失败回退五个阶段执行。调度器先依据[[MATH:AID]]、[[MATH:FID]]读取风险、覆盖率、不确定性、负证据压力及预算，构造[[MATH:P_feasible]]并执行[[MATH:p^*]]；执行器记录证据包版本、成本、时延和校验状态。补证后重新选路，可修复错误最多重写一次，预算不足或强校验失败则降级。")
add_supplement("文字路由与效用公式关系", "低风险、低覆盖率或字段不完整的告警进入轻量模板路径", "低风险、低覆盖率或字段不完整的告警进入轻量模板路径", "该段文字是路径可行性约束和初始筛选规则，不是与效用函数并列的第二套决策。系统先据字段完整性、强校验要求和预算生成[[MATH:P_feasible]]，再计算[[MATH:U(a,p)=G(a,p)-lambda C(a,p)-rho H(a,p)-eta D(a,p)]]，并以[[MATH:p^*=argmax_(p in P_feasible)U(a,p)]]选路。硬规则只允许一条路径时直接执行；存在多条可行路径时由效用值决定。")
add_supplement("协议侧信道术语落点", "系统为每个流对象抽取包级特征、流级特征和协议侧信道特征", "协议侧信道特征", "此处术语应统一解释为“协议可见侧信道特征”，指不解密应用载荷即可从协议首部、握手过程和关联元数据取得的属性；其与后文[[MATH:X_protocol]]为原始特征集合和模型输入向量的关系。")
add_supplement("等级风险映射步骤", "若某检测器仅输出等级，则根据等级映射表转换为数值风险", "若某检测器仅输出等级，则根据等级映射表转换为数值风险", "具体步骤为：读取[[MATH:detector_id]]、原始等级和[[MATH:calibration_version]]，查询仅由训练集或验证集形成的版本化映射表，得到[[MATH:r_a in[0,1]]]；未登记等级写入缺失状态并转校准或降级流程，不得凭固定默认值替代。")


marked = [item for item in specs if item["kind"] == "marked"]
supplement = [item for item in specs if item["kind"] == "supplement"]
assert len(marked) == 154, len(marked)
assert len(supplement) == 26, len(supplement)

for index, item in enumerate(marked, start=1):
    item["id"] = f"M{index:03d}"
    item["text"] = f"[标记事项{index:03d}/154：{item['title']}] {item['text']}"

for index, item in enumerate(supplement, start=1):
    item["id"] = f"S{index:02d}"
    item["text"] = f"[补充事项{index:02d}/26：{item['title']}] {item['text']}"

OUTPUT.parent.mkdir(parents=True, exist_ok=True)
OUTPUT.write_text(json.dumps(specs, ensure_ascii=False, indent=2), encoding="utf-8")
print(f"OUTPUT={OUTPUT}")
print(f"MARKED={len(marked)}")
print(f"SUPPLEMENT={len(supplement)}")
print(f"TOTAL={len(specs)}")
