# 当前实验进度、数据位置与论文结论边界（2026-07-20）

## 1. 总体判断

当前实验已经能够支撑论文主体撰写，但不能支撑摘要、最终主结果表和结论中的“确认性全面 SOTA”表述。

可以定稿的内容包括：问题定义、多模态证据与冲突建模、无泄漏 strict-v4 协议、7 个核心数据集与 102 个开放集场景、seed7 的 29 方法开发表、经典与现代强基线、D1-D7 冲突度量、F2-F9 融合算子、组件消融、负结果和复现约束。

暂不能定稿的内容包括：多种子确认性主表、胜出算法相对 OpenDetect 的独立外部确认、统一效率比较和最终综合 SOTA 审计结论。最终自有算法已冻结为 `caeos_pairwise`，DoHBrw2020 描述性外推也已完成。

## 2. 实时运行状态

冻结快照时间：北京时间 `2026-07-20 09:20`。

| 实验链 | 完成情况 | 失败 | 当前判定 |
|---|---:|---:|---|
| Router CAEOS 分支 | `306/306` | 0 | 已完成第一阶段 |
| Router MLP/OpenMax 分支 | `306/306` | 0 | 已完成；Router 严格替换门 FAIL |
| Tail-aware 三种子确认 | `306/306` | 0 | 严格确认门 FAIL |
| 自有算法锦标赛 | 已完成 | 0 | 冻结选择 `caeos_pairwise` |
| 胜出算法外部确认 | `28/306` | 0 | OpenDetect 独立种子确认运行中 |
| DoHBrw2020 扩展 | `9/9` | 0 | 描述性外推已完成 |
| Mahalanobis++ | 试点 `14/14`、扩展 `102/102` | 0 | 29 方法表完成；OpenDetect 仍最强外部比较器 |
| 综合 SOTA 审计 v10 | 等待锦标赛与外部确认 | - | watcher PID `3615388` |

Router 不是单个 `306` 任务：CAEOS 与 MLP/OpenMax 两个根目录各完成 `306` 个运行。Router 和 Tail-aware 的端到端完成标记均已生成，但严格替换门均失败；预注册锦标赛已冻结选择 `caeos_pairwise`。

## 3. 已完成的关键实验

1. strict-v4 开发屏幕：7 个核心数据集、102 个场景、28 个方法的 seed7 主表和完整 SHA/拆分审计已完成。
2. 训练型强基线：CLOSR/CADE/Sieve `42/42`、ARPL/PALM/RoNeTC/FOSS `56/56`、AEGIS `14/14`，均零失败且无未知/测试标签选模；所有候选均按冻结门关闭 full102 扩展义务。
3. 闭集强基线：Mal_TLS2023 XGBoost 2.1.4 五种子 `5/5` 完成，Macro-F1 为 `0.974380±0.002474`，未超过 MC7-Stable。
4. 冲突机制：D1-D7 已覆盖 102 场景。D6 条件归一化冲突相对原始 DS 的平均 AUROC 增益为 `+0.143181`，bootstrap 95% CI 为 `[+0.083157,+0.203062]`，配对 Wilcoxon `p=1.47e-6`。
5. 融合机制：F2/F3/F4/F5/F6/F9 已完成 102 场景和 306 项源 SHA 检查。F9 完整 CAEOS 的四项未知指标平均秩为 `1.00`；轻量可学习注意力 F3 为 `5.75`，未进入最终算法。
6. AEGIS：在 14 个冻结场景上平均优于 OpenDetect，但 CIC-ToN-IoT 与 USTC-TFC 的套件增益分别为 `-0.095161/-0.100566`，仅 4/7 套件非负，套件稳健性门失败。
7. Mahalanobis++：ICML 2025 方法先完成冻结 `14/14` 试点并通过扩展门，再完成 `102/102` 全屏幕。AUROC/AUPR/FPR95/OSCR 为 `0.727044/0.529891/0.485973/0.563717`，四指标平均秩 `7.25`，优于标准 Mahalanobis 的 `8.50`，但未超过 OpenDetect 的 `6.50`。

## 4. 当前实验结论

### 4.1 自有算法

开发阶段最有证据支持的机制仍是“条件归一化冲突 + 可靠度污染折扣”。领域安全 Router 与 Tail-aware 的新种子确认均显示四项未知指标平均改善、Known F1 不降，但前者未同时满足 bootstrap、Holm 与全套件非回退要求，后者的 AUPR 95% CI 下界为负且三个套件存在回退。两条替换门均 FAIL，预注册锦标赛已将最终自有算法冻结为 `caeos_pairwise`。

### 4.2 外部基线

OpenDetect 仍是冻结的最强外部比较器。新增 Mahalanobis++ 在 102 场景中稳定改善标准 Mahalanobis，但 29 方法表平均秩仍低于 OpenDetect，因此不需要推翻既有外部确认协议。AEGIS 在小型冻结试点上的平均指标较强，但存在明显跨套件回退；ARPL、PALM、RoNeTC、FOSS、CLOSR、CADE 和 Sieve 均未通过扩展门。论文应同时报告这些负结果，不能只保留有利方法。

### 4.3 机制解释

D6 证明条件化冲突比原始 DS 更有效，且增益不是简单由平均不确定性替代；D7 可靠度加权相对 D6 仅在 50% 场景改善，因此可靠度应作为污染鲁棒性折扣，而不能宣称普遍提高未知排序。F3 注意力只改善校准，没有改善整体未知检测，不能作为 Transformer/SOTA 创新点。

### 4.4 效率边界

历史 seed7 结果的计时语义不可比。新执行就绪审计显示，Pairwise 候选运行覆盖 `102/102`，但可加载模型为 `0/102`；OpenDetect 为 `102/102` 和 `102/102`。因此冻结效率协议 v1 的“仅用保存模型、禁止 refit”条款当前不可执行，严禁把 `scores.npz` 后处理耗时当成端到端模型推理。最终算法确定后，必须在零效率结果条件下冻结选中分支专用的 v2 受控重放与插桩协议。

## 5. 实验数据位置

### GPU 服务器原始运行与大产物

- 项目根目录：`/opt/data/private/wangwt/ParkAttackKE/CAEOS-EMTD-strict-v4-20260717`
- Router CAEOS：`runs/strict_v4_domain_safe_router_confirmation_caeos`，快照占用约 `912 MB`
- Router MLP/OpenMax：`runs/strict_v4_domain_safe_router_confirmation_mlp`，快照占用约 `226 MB`，仍在增长
- Tail-aware：`runs/strict_v4_tail_aware_confirmation`，快照占用约 `1.2 GB`
- 独立 OpenDetect 外部确认：`runs/strict_v4_external_comparator_confirmation`
- seed7 Pairwise 主源：`runs/strict_v4_full103_pairwise_caeos_seed7`
- seed7 外部基线：`runs/strict_v4_full103_independent_baselines_seed7`
- 冻结协议、汇总、门控和 watcher 日志：`results/`
- 数据集与缓存：项目根目录下 `caches/`，以及 `/opt/data/private/wangwt/ParkAttackKE/datasets/`

GPU 端保存逐场景 `metrics.json`、`scores.npz`、`evidence_package.npz`、`provenance.json`、模型权重和运行日志。大文件不回传本地仓库。

### 本地轻量证据与论文报告

- 算法代码与轻量结果：`source/CAEOS-EMTD`
- 统一轻量结果入口：`source/CAEOS-EMTD/results/`
- 28 方法开发主表：`source/CAEOS-EMTD/results/strict_v4_mandatory_scores_full102_seed7/summary.json`
- 29 方法 Mahalanobis++ 扩展表：`source/CAEOS-EMTD/results/strict_v4_mahalanobis_pp_full102_seed7/`
- 风险覆盖：`source/CAEOS-EMTD/results/strict_v4_risk_coverage_seed7/analysis.json`
- D1-D7 冲突专项：`source/CAEOS-EMTD/results/strict_v4_conflict_metrics_seed7/`
- F2-F9 融合专项：`source/CAEOS-EMTD/results/strict_v4_fusion_operators_seed7/`
- AEGIS 试点：`source/CAEOS-EMTD/results/strict_v4_aegis_training_pilot_seed7/`
- Tail 确认与锦标赛：`source/CAEOS-EMTD/results/strict_v4_tail_aware_confirmation/`、`strict_v4_optimal_self_algorithm/`
- 外部确认协议与进度：`source/CAEOS-EMTD/results/strict_v4_external_confirmation/`
- 效率可执行性审计：`source/CAEOS-EMTD/results/strict_v4_final_efficiency_readiness_seed7/readiness.json`
- 论文方向轻量镜像与索引：本方向目录的 `results/` 和 `03_实验报告/`

## 6. 剩余必须完成的实验

| 优先级 | 实验 | 完成条件 |
|---|---|---|
| 完成 | Tail-aware 收口 | `306/306`、失败 0；严格确认门 FAIL |
| 完成 | 冻结自有算法锦标赛 | 按预注册门冻结选择 `caeos_pairwise` |
| P0 | 胜出算法相对 OpenDetect 的独立确认 | 使用协议指定的新种子；当前 `28/306`，需完成配对统计、套件下界和无泄漏检查 |
| 完成 | DoHBrw2020 描述性外推 | `9/9`、零失败；与核心 7 数据集结论分开 |
| P0 | 综合 SOTA 审计 v10 | 新增 29 方法 Mahalanobis++ 屏幕硬门；其余协议 SHA、统计、无泄漏和最终标记全部通过 |
| P1 | 选后 strict-v4 污染确认 | 对冻结胜出算法按污染家族定义强度；与 MC8 已知类污染证据分开报告 |
| P1 | 最终效率 v2 | 相同硬件和语义下测训练、校准、P50/P95/P99、吞吐、显存和主存 |
| P1 | 论文最终统计表 | 场景配对 bootstrap CI、效应量和多重比较校正 |

结论：现在应继续撰写论文主体，并把最终确认性措辞留空；锦标赛和 DoH 已完成，只有独立外部确认、效率证据和综合审计通过后，才能决定是否使用“全面 SOTA”。

## 7. 06:05 三矩阵盲态健康更新

运行健康审计已升级为 `strict_v4_running_confirmation_health_audit_v2`，不读取或聚合任何测试指标值。北京时间 `2026-07-20 06:05:30` 的权威快照为：Router CAEOS `306/306`、Router MLP/OpenMax `274/306`、Tail-aware `280/306`，失败、非法路径和报告错误均为 0，整体健康门 PASS；两组在跑矩阵分别还剩 `32` 和 `26` 个任务。该审计同时验证 MLP `model.pt`、OpenMax 报告、拆分指纹及无未知/测试标签拟合标记，解决了旧版只报告 Router CAEOS 第一阶段的问题。5 分钟盲态 watcher 已升级为 PID `2715075`，遇到并发写入造成的瞬时 JSON 读取失败会记录后重试，不会误停监控；截至本快照错误日志为空。轻量证据位于 `source/CAEOS-EMTD/results/strict_v4_running_confirmation_health/health.json`，当前 JSON SHA256 为 `0b6adb3fb96d71052bcec0c9987a65377b436cde692ac49575cdb64f34eaf280`。

## 8. 07:43 Router、DoH 与 29 方法更新

Router 两阶段均已完成 `306/306`，但确认性替换门 FAIL，预注册 incumbent 回退为 `caeos_pairwise`。DoHBrw2020 描述性屏幕完成 3 场景 x 3 种子共 `9/9`：Pairwise 的 Known F1/AUROC/AUPR/FPR95/OSCR 为 `0.975995/0.907012/0.935702/0.301177/0.883466`，该结果不用于反向调参，也不并入七数据集确认性结论。Mahalanobis++ 完成 `14/14` 冻结试点和 `102/102` 扩展，29 方法表仍选择 OpenDetect。07:43 时 Tail-aware 为 `296/306`、失败 0；其后续终态见第 9 节。

## 9. 09:20 Tail 终态、锦标赛与外部确认

Tail-aware 已完成 `306/306`、失败 0。相对冻结 reference 的 AUROC/AUPR/FPR95/OSCR 有向增益为 `+0.008555/+0.008218/+0.061314/+0.026340`，Known F1 不变；但 AUPR 95% CI 为 `[-0.006201,+0.020284]`，且 CIC-IoT、CIC-ToN、NF-UNSW 存在套件回退，严格确认门 FAIL。锦标赛随即冻结选择 `caeos_pairwise`。其对 OpenDetect 的独立种子外部确认已启动，当前 `28/306`、失败 0；综合 SOTA 审计 v10 继续等待该矩阵完成。

## 10. 10:12 ExCeL、30 方法表与效率 v2 更新

ExCeL 正式屏幕已完成七套件 `102/102`、失败 0，612 项产物检查、102 项拆分指纹检查和无未知/测试标签选参检查全部通过。其 Known F1/AUROC/AUPR/FPR95/OSCR 为 `0.721949/0.700213/0.510958/0.519102/0.552586`，四项未知指标平均秩 `11.50`；OpenDetect 为 `6.50`，所以冻结外部比较器保持不变。轻量结果位于 `source/CAEOS-EMTD/results/strict_v4_excel_full102_seed7/`，GPU 完整结果位于 `results/strict_v4_excel_full102_seed7/`。

综合审计已升级 v11，新增 ExCeL 30 方法完整性、29 方法指标继承、协议 SHA 和比较器不变检查；`21/21` 测试与 Bash 静态检查通过，新 watcher PID 为 `3892820`。独立外部确认当前 `84/306`、失败 0。效率 v2 生成器及 v1 回归共 `9/9 PASS`，但正式协议必须等外部确认完成后在零效率结果状态冻结，因此当前仍不得写端到端效率优势或“全面 SOTA”。

选后污染协议也已在零结果状态冻结，SHA 为 `83415875d1f26c8f1c948dac65f498110a5f3a6080e2aba4fd4407aa05eea4f4`。它固定五类测试时污染、3 个模态、7 个哨兵强度曲线 `273` 次与 full102 确认 `510` 次，总计 `783` 次；协议和 runner 测试 `7/7 PASS`。完整协议位于 GPU 与本地的 `results/strict_v4_postselection_corruption_seed7/`。执行顺序冻结为外部确认、独占效率 v2、污染确认，三者不得并发。
