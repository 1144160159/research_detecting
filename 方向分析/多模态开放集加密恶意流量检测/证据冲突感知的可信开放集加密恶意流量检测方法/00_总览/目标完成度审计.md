# 目标完成度审计

审计对象为稳定版本`v1.4.4`及其实现`source/CAEOS-EMTD`。本文件按总目标逐项给出代码、实验和可复现产物，不以单一测试通过替代功能证明。

## 1. 不依赖载荷解密

**结论：已实现。** 数据入口读取流级CSV特征，模态由配置文件明确列出。使用的“payload statistics”是包载荷长度、包数、速率和到达间隔统计，不读取加密内容、会话密钥或解密后的字节。数据预处理器只在已知训练集拟合，并排除标签与直接标识字段。

核验位置：[数据加载与分组划分](../../../../source/CAEOS-EMTD/caeos/data.py)、[Mal_TLS模态配置](../../../../source/CAEOS-EMTD/configs/mal_tls2023.json)、[HIKARI模态配置](../../../../source/CAEOS-EMTD/configs/hikari2021.json)、[DoH模态配置](../../../../source/CAEOS-EMTD/configs/dohbrw2020_multiclass.json)。

## 2. 稳定识别已知恶意类别

**结论：已实现并跨数据集验证。** MC7使用随机森林与ExtraTrees的验证集选择组合建立已知类主干，独立模态只在已知验证Macro-F1达到最低增益时参与分类融合。Mal_TLS完整数据seed7的Macro-F1为`0.986765`；HIKARI、DoH和Mal_TLS统一39任务的平均Known Macro-F1为`0.974016`。

核验位置：[混合分类器](../../../../source/CAEOS-EMTD/caeos/hybrid.py)、[多分类实验报告](../03_实验报告/02_基线对比/MC7优化与论文最佳实验对比报告.md)、[外部强基线结果](../03_实验报告/02_基线对比/CADE统一协议强基线对比报告.md)。

## 3. 每模态独立证据、可靠度和不确定性

**结论：已实现。** 每个模态具有独立ExtraTrees分类器并输出类别概率；类别证据由类别概率、局部置信度和验证可靠度共同形成；归一化熵作为模态不确定性；验证性能与样本置信度共同形成模态可靠度。神经证据实现还保留独立Dirichlet证据头，用于机制消融。

核验位置：[稳定混合证据实现](../../../../source/CAEOS-EMTD/caeos/hybrid.py)、[Dirichlet证据实现](../../../../source/CAEOS-EMTD/caeos/model.py)、[证据输出测试](../../../../source/CAEOS-EMTD/tests/test_multiclass.py)。

## 4. 显式冲突与模态污染鲁棒性

**结论：已实现并完成专项实验。** 稳定主干计算任意模态对的Jensen-Shannon冲突矩阵，并用模态可靠度形成全局和局部冲突；MC8-v2在模态缺失、置乱和污染时执行冲突驱动折扣和保守路由。冲突被保留为独立输出和开放集风险分量，不在融合中被无痕消除。

核验位置：[冲突矩阵与可靠度门](../../../../source/CAEOS-EMTD/caeos/hybrid.py)、[模态污染实现](../../../../source/CAEOS-EMTD/evaluate_hybrid_corruption.py)、[MC8实验报告](../03_实验报告/03_专项与阶段/MC8模态污染鲁棒性实验报告.md)。

## 5. 不使用真实未知标签调参

**结论：已实现并通过嵌套协议验证。** 外层未知类别完整移出训练和已知验证集。支持风险、冲突风险和联合风险的路径选择只在已知攻击类别中执行内层留一，将一个已知攻击类暂作伪未知；稳健目标为平均AUROC与最小AUROC等权。最终阈值只取独立已知验证风险分位数。外层未知标签仅用于最后计算测试指标。

核验位置：[嵌套风险选择](../../../../source/CAEOS-EMTD/train_hybrid_open_set.py)、[层级门控测试](../../../../source/CAEOS-EMTD/tests/test_hierarchical_fallback.py)、[NF-UNSW独立确认](../03_实验报告/01_主线验证/NF-UNSW层级联合门控实验报告.md)。九个新种子54次全规模确认中，v1.4.4相对v1.4.3为12胜41平1负，AUROC提高`0.015589`，精确Wilcoxon `p=0.001709`。

## 6. 可解释、可校准、可复现输出

**结论：已实现。** 每次运行输出`metrics.json`、研究评估用`scores.npz`和不含测试真值的`evidence_package.npz`。证据包逐样本保存模态名称、类别证据、类别意见、模态不确定性、可靠度、局部冲突、成对冲突矩阵、融合概率、风险名称、已知验证阈值和最终拒识决定。独立校验器检查字段、形状、概率归一性、有限值、真值隔离及风险阈值一致性。

核验位置：[证据包生成](../../../../source/CAEOS-EMTD/train_hybrid_open_set.py)、[证据包校验器](../../../../source/CAEOS-EMTD/verify_evidence_package.py)、[校验器测试](../../../../source/CAEOS-EMTD/tests/test_evidence_package.py)。真实HIKARI小规模验证包包含50个样本、4个模态、4个已知类和49个字段，未包含`test_labels`或`test_unknown`，26个拒识决定与风险阈值逐项一致。

## 7. 综合判定

六项总目标均已有直接代码实现、自动化测试和至少一类实验或运行产物支撑。当前方法可以定义为：**一种不解密载荷、由独立侧信道模态形成证据意见、显式建模可靠度与冲突、利用已知类内层留一选择支持/冲突/联合风险，并输出可审计证据包的可信开放集加密恶意流量检测方法。**

完成总目标不等于解决所有研究边界。Analysis等高度重叠家族仍有较低AUROC；CLOSR在六个任务、Open-Detect在七个任务、FOSS在五个任务、Sieve在两个任务中占优。Open-Detect、RoNeTC、FOSS和Sieve已经完成统一侧信道适配；FOSS结构路线和Sieve固定融合均未同时改善总体AUROC与OSCR。原始字节图像复现、Sieve混合标签噪声原协议、M3S-UPD半监督持续更新、TAO-Net的LLM未知细分类和SiamXBERT少样本适配仍属于不同协议。后续工作属于论文扩展、跨来源验证和性能提升，不构成上述六项能力的实现缺口。

## 8. strict-v4 全面 SOTA 目标审计

**结论：基线完整，确认尚未完成。** strict-v4 已覆盖 7 数据集、102 个细粒度留一攻击场景和 22 种方法；OpenDetect 与四类经典 OOD 的独立运行共 204/204，失败 0。最终总表通过 1326 项产物检查和 102 组拆分指纹一致性检查。

领域安全路由在 seed7 完整屏幕上的 AUROC/AUPR/FPR95/OSCR 为 `0.777024/0.595685/0.465323/0.635143`，四指标平均秩 `1.50`；最强非 CAEOS 方法 OpenDetect 的对应结果为 `0.733439/0.525633/0.503411/0.591630`，平均秩 `6.00`。seed7 参与路由开发，不能作为确认性结论。

种子 `137/139/149` 的领域安全路由 102 场景冻结确认已经启动。尾部感知连续排序头在 14 个 seed7 困难场景上的四项有向均值增益为 `+0.031778/+0.026099/+0.205403/+0.093042`，通过开发门后已在任何新种子运行前冻结 `157/163/167`、102 场景、306 次确认协议。候选间条件锦标赛协议 SHA `8db2551480895a1911ce5cd36970463a662c0a44d4823ffb55273746cffce851` 已冻结；尾部若胜出，必须再用完全独立的 `173/179/181` 相对 OpenDetect 确认。只有内部候选确认、候选间同拆分直接比较、相对冻结 OpenDetect 的外部确认、DoH 描述性扩展和综合 SOTA 审计全部通过，才允许将结论升级为 `confirmed_external_sota_7_datasets_102_scenarios`。当前状态仍为 **全面 SOTA 未证实**。

核验位置：[strict-v4阶段报告](../03_实验报告/01_主线验证/strict-v4全面SOTA与自有算法优化阶段报告.md)、`source/CAEOS-EMTD/results/strict_v4_full103_seed7/summary.json`、`source/CAEOS-EMTD/results/strict_v4_external_confirmation/protocol_manifest.json`。

### 8.1 现代 OOD 基线补充

在原 22 方法基础上，ReAct、DICE、SHE 已复用冻结 MLP 检查点完成 102 场景、306/306 个新增报告；SIRC-MSP-Residual、Shannon Entropy 和独立最近类中心 Prototype 又完成 full102，当前完整开发表为 28 方法。新增方法均未超过 OpenDetect 的四指标平均秩 `6.00`，因此 OpenDetect 仍是预注册规则下最强非 CAEOS 比较器。DOC-Fixed 14 场景试点未过扩展门；CLOSR/CADE/Sieve 独立训练试点已 `42/42` 完成且无泄漏检查通过，三者均未过预冻结扩展门，因此没有 full102 扩展义务，OpenDetect 仍为最强外部比较器。

截至北京时间 2026-07-20 04:38，领域路由 CAEOS 阶段已 `306/306`，MLP/OpenMax 阶段 `104/306`，尾部感知确认 `267/306`，失败均为 0；第一组和互补组训练强基线均已完成且无 full102 义务，AEGIS clean-label 为 `9/14`。F2-F9 融合矩阵已完成，综合审计 v9 可验证训练扩展与融合交叉一致性。全面 SOTA 仍须等待两条自有算法确认、冻结锦标赛、AEGIS 及可能的扩展义务清零、相对最强外部比较器的独立确认和综合审计 v9。核验位置：[一周实验进度与论文支撑性审计](../03_实验报告/01_主线验证/一周实验进度与论文支撑性审计_2026-07-19.md)。

### 8.2 闭集 C2 强基线补齐

实验矩阵要求 XGBoost/LightGBM 至少完成其一。现已在 Mal_TLS2023 每类最多 500 条、23 类、种子 `7/11/19/23/29` 的同采样与划分配置上完成 XGBoost 2.1.4。协议在正式结果为 0 时冻结，五次运行全部完成、失败 0；已知验证集 mlogloss 只用于 early stopping，测试标签不参与拟合或选择。XGBoost Macro-F1 为 `0.974380±0.002474`，比同种子 MC7-Stable 平均低 `0.000766`；ECE/NLL 分别高 `0.002623/0.006154`，MC7-Stable 仍是闭集主干。该证据只补齐闭集表格强基线，不参与 strict-v4 开放集 SOTA 排名。核验位置：[XGBoost五种子闭集基线报告](../03_实验报告/02_基线对比/Mal_TLS2023_XGBoost五种子闭集基线报告.md)。

### 8.3 当前机器总账与PUG跨套件边界

截至2026-07-27本次核验，正文经典基线已冻结为MSP、Energy、OpenMax、kNN、ViM、Mahalanobis++和OpenDetect共7项；Pairwise仅对其中3项实现五指标严格支配，对其余4项均受FPR95约束。因此“基线选择完成”不等于“全面SOTA完成”。

KRC权威原子进度仍为60/306，尚无`progress_064.json`。自有算法终选必须完整经过KRC/RRC条件链和PUG链：KRC阴性后不能跳过RRC，PUG 18任务阳性后不能跳过fresh跨套件确认。后者已结果前冻结为7套件102场景、306个Pairwise/PUG候选任务与306个fresh OpenDetect任务，设计canonical/file为 `4cb8770e...e693/4d14ea95...5113`；但执行授权为false、正式结果为0，不能构成效果证据。

当前总账canonical/file为 `c4c4d745...5c81/e6e5f9a3...4ba3`，GPU审计回归 `6/6 PASS`。总账仍列8项阻断：KRC部分进度、PUG 18任务无终态、五指标严格支配仅3/7、绝对污染门失败、相对OpenDetect污染门失败、外部恶意确认缺失、PARROT良性安全缺失和selected-system效率比较缺失。当前选中算法仍为Pairwise，`goal_achieved=false`，全面SOTA结论未授权。

### 8.4 64项检查点与跨套件实现准备度

最新KRC原子进度为64/306，64份capture与execution、384份六条件evaluation完全对齐，routing为0/64。Known F1均值0.609367且0/64达到绝对门，error-AUROC均值0.704999且37/64达到0.7；21个完整场景中13个具RRC诊断资格。该证据继续支持保留RRC条件路线，但仍不足以提前判定KRC终态。

PUG阳性后的跨套件链已有7项中的2项实现：canonical激活决策器与条件执行协议生成器。隔离GPU设计+激活+协议测试 `13/13 PASS`，真实状态只返回pending，activation、protocol及任务结果均不存在。机器审计记录ready=2/7、complete=false，且明确工程准备不属于效果。

刷新后的总账canonical/file为 `f70bbf58...d2d6/45c4dd25...b361`，审计GPU回归 `7/7 PASS`。阻断总数仍为8，当前选中算法仍是Pairwise，`goal_achieved=false`。

### 8.5 PUG跨套件执行准备度4/7

跨套件runner与逐任务evaluator已经补齐：runner固定7套件、3新种子、2x8资源和306 PUG/306 fresh OpenDetect任务；evaluator要求每个配对任务7工件齐全、拆分与test数组一致、选择无泄漏，并独立重算指标。隔离GPU五段链 `28/28 PASS`，但正式协议和任务均不存在。

机器总账现记录PUG实现ready=4/7、complete=false，剩余summary、独立audit和conditional watcher。最新总账canonical/file为 `a3d6b98f...2d6e/8115a91d...ff2f`；KRC仍64/306，8项阻断和`goal_achieved=false`不变。

### 8.6 PUG实现7/7与KRC 68项检查点

PUG跨套件summary、独立audit和conditional watcher已补齐，连同design、activation、protocol、runner和evaluator形成7/7工程闭环。结果前冻结的稳定覆盖要求为每场景至少2/3种子实际选择PUG、每套件至少1个启用场景、总体至少18场景和4套件；统计按7套件等权，bootstrap不参与选择。GPU隔离八段链 `40/40 PASS`，真实入口仍等待pilot且未启动训练。代码未进入远端主目录或staging，正式activation、protocol、task、summary、audit和completion均为0。

KRC最新canonical快照为68/306 capture、408/1836 evaluation；Known F1均值0.608843且0/68达到0.9，error-AUROC均值0.704059且38/68达到0.7，22个完整场景中13个具RRC资格。progress canonical/file为 `15c75a4e...206f9/d79ccbe7...da39`，known-only audit为 `b13d12ba...8392e/776daeef...dc6f`。该结果仍不足以决定KRC/RRC分支。

机器总账canonical/file更新为 `91e81c6e...191f8/c3d65166...139da`。PUG implementation complete与PUG effect terminal被机器字段明确分离；八项阻断仍是KRC部分进度、PUG无终态、Pairwise严格五指标支配仅3/7、两项污染门失败、外部恶意、PARROT良性安全和效率结果缺失。PARROT良性安全用于评估跨域未知良性的误报和攻击误指派，不参与攻击类别主表。当前算法仍为Pairwise，`goal_achieved=false`，全面SOTA未证实。

### 8.7 双阳性自有算法直接锦标赛

PUG跨套件终态现由protocol、summary、独立audit和completion四工件共同定义，阴性保留上游、阳性在上游为Pairwise时可终选。若KRC或RRC也阳性，PUG只成为provisional challenger；没有同拆分直接比较时，最优自有算法仍为未完成。

结果前锦标赛设计已冻结为7套件102场景、全新seeds809/811/821、306个配对任务和三条件918组evaluation。它采用incumbent-challenger规则，只有PUG同时通过Known F1保护、未知指标改善、bootstrap下界、5/7套件广度、最差套件和污染条件门才替换KRC/RRC。设计canonical/file为 `258605ec...6684b/7d313c2f...40b0f`，正式输出0、执行授权false。

GPU目标审计与设计回归 `15/15 PASS`。新总账canonical/file为 `c724b40a...bd67c/1ae7e6c4...1fa06`，记录`direct_tournament_required=false`、`design_frozen=true`；当前尚未出现双阳性，因为KRC仅68/306且PUG未执行。8项阻断和`goal_achieved=false`不变。

外部恶意、PARROT良性安全和同硬件效率的现有协议仍仅适配KRC。若最终算法为RRC或PUG，必须通过新的selected-system适配器执行同等证据链；KRC目录改名或复用不构成有效结果。

### 8.8 KRC 72项更新

最新原子进度为72/306 capture与432/1836 evaluation。Known F1均值0.607964且0/72达到0.9，error-AUROC均值0.703922且40/72达到0.7；24个完整场景中14个具RRC资格，新增资格场景为Mirai-UDPPlain。progress canonical/file为 `dbe377e6...c896b/b8a4dc86...19d37`，known-only audit为 `9b4fc70e...cc633/1187bc59...2c7d6`。

机器总账canonical/file更新为 `529d6b75...77ff2c/29bc1496...f56600`。KRC部分进度阻断更新为72/306，其他7项、总阻断数8、Pairwise当前选择和`goal_achieved=false`不变。

### 8.9 selected-system统一运行时与下游设计

Pairwise运行时现可序列化PUG连续outer-min-p风险，新的统一适配器支持Pairwise、KRC、RRC和PUG，并对算法身份、预测/概率/risk合同及known-validation阈值隔离执行失败关闭。结果前设计canonical/file为 `f69a781d...5531f/c6f9eb29...26738`，共同运行时完成，但正式输出为0、执行授权false，仍有6项算法无关下游组件待实现。

GPU隔离联合测试 `33/33 PASS`，Windows与GPU重建工件逐字节一致。机器总账新增设计冻结与共同运行时完成字段，并明确它们不代表外部恶意、PARROT安全、效率或综合SOTA效果。刷新后的总账canonical/file为 `fd0f24e9...f048/172b8e7d...faa6`，仍有8项阻断；KRC 72/306、Pairwise当前选择、`goal_achieved=false`和全面SOTA未授权均不变。

### 8.10 KRC 76项更新

最新原子进度为76/306 capture与456/1836 evaluation，70 primary+6 secondary、routing 0/76且九项不变量全部通过。Known F1均值0.608325且0/76达到0.9，error-AUROC均值0.704102且43/76达到0.7；25个完整场景中15个具RRC资格，新增MITM ARP Spoofing。progress canonical/file为 `a7c9e904...8198/aecad5fc...50d7`，known-only audit为 `c35b8586...e67e/e912eb67...d23d`。

机器总账canonical/file更新为 `3e2bd629...d456/0afa32de...a73e`。KRC部分阻断更新为76/306，其他7项、总阻断数8、统一适配器已冻结/运行时完成、Pairwise当前选择和`goal_achieved=false`不变。

### 8.11 selected-system激活准备度1/6

最终自有算法激活器已实现，要求canonical总账终选、四处算法身份一致、selection snapshot SHA和激活前正式输出全0。GPU统一适配链 `41/41 PASS`；真实当前状态只返回pending，未生成activation。

机器总账新增selected-system实现清单，当前ready=1/6、complete=false。新总账canonical/file为 `5b995611...02bf/22377f69...64a7`；8项科学阻断、KRC 76/306、Pairwise当前选择和`goal_achieved=false`不变。脚本就绪不等于最终算法已激活，也不等于任何下游效果完成。

### 8.12 统一external准备度2/6与KRC 80项

selected-system的算法无关external协议和运行器已实现，覆盖Pairwise、KRC、RRC与PUG，固定LSNM2024/CICDDoS2019共32攻击家族、3种子和96个fresh split。四候选统一使用`prediction/probability/risk`合同和fresh OpenDetect；RRC必须以攻击家族为块汇聚三个known-only种子证书。训练参数、捕获工件、源metrics/scores、输入数组和最终metrics均SHA绑定，test标签只用于最终指标。

运行器/测试SHA `9e5af89e...cafc3/283d6c8a...a352`，隔离GPU联合目标总审计回归`31/31 PASS`。真实状态仍因最终自有算法未选择而pending，formal external protocol与效果结果为0；实现清单ready=2/6、complete=false。

KRC权威进度为80/306 capture、480/1836 evaluation，routing 0/80。Known F1均值0.608904且0/80达到0.9，error-AUROC均值0.704204且46/80达到0.7；26个完整场景中16个具RRC资格。progress canonical/file `fd5e9786...1cf49/f339bc93...e7e7d`，bottleneck `f935810f...558fc/e6de6112...2e16a`。

机器总账canonical/file更新为`f622ff2f...f0f5/347f5dd5...87e2`。八项阻断未减少：KRC未终态、PUG未终态、Pairwise仅严格支配3/7经典基线的五指标、两类污染门失败、外部恶意效果/PARROT良性安全/效率终态缺失。因此`goal_achieved=false`、Pairwise仍为当前incumbent，全面SOTA未授权。

### 8.13 统一PARROT准备度3/6与KRC 88项

selected-system的算法无关PARROT安全组件已实现，覆盖Pairwise、KRC、RRC与PUG。它固定USTC-TFC2016的10场景乘3种子源矩阵，候选对每个源拆分fresh refit，PARROT不得参与训练、终选、校准或阈值；320个capture按30个源模型对平均后做capture-block bootstrap和80应用长尾汇总。运行器绑定算法身份、56列特征、分片、预处理、源拆分、候选/OpenDetect工件和最终指标SHA，拒绝跨算法改名、源分割拼接及结果后调参。

运行器/测试SHA `6cdecf59...587a1/98ad9c3b...dfce`；新增测试`13/13 PASS`，隔离GPU组合回归`44/44 PASS`。真实GPU主项目仍无PARROT完整feature summary和selected-system activation，入口返回pending且formal protocol/效果结果为0。本地/隔离staging实现清单ready=3/6、complete=false；GPU主目录只同步了新增runner/test，没有覆盖运行中的trainer或完整`caeos`适配器，因此3/6不等于主项目已激活。

KRC权威进度为88/306 capture、528/1836 evaluation，82 primary+6 secondary、routing 0/88。Known F1均值0.610638且0/88达到0.9，error-AUROC均值0.703751且49/88达到0.7；29个完整场景中18个具RRC资格。progress canonical/file `866eb568...a9fd/2712cb0e...527c`，bottleneck canonical/file `b12e4f3a...b3ad/97766b82...9f65`。

本地/隔离staging机器总账canonical/file为`945a90ee...c68d/7c150475...47de`。八项阻断仍是KRC未终态、PUG未终态、Pairwise严格五指标支配仅3/7、绝对与相对OpenDetect污染门失败、外部恶意效果缺失、PARROT良性安全缺失和selected-system效率缺失。Pairwise仍为provisional incumbent，最优自有算法未终选，`goal_achieved=false`，全面SOTA未授权。

### 8.14 统一效率准备度4/6与KRC 96项

已实现Pairwise、KRC、RRC和PUG共用的selected-system同硬件效率运行器。最终候选、其同一拟合状态中的嵌入Pairwise以及fresh OpenDetect都使用完整7套件、102场景、训练seed `647/653/659` 共306个源拆分；每个batch都向三者传入逐数组相同的输入，在同一进程轮转计时。固定batch `1/64/512`、warmup 5、timed 30，报告p50/p95/p99、吞吐、host RSS、GPU峰值、工件字节和完整capture墙钟；统计先对场景内3种子平均，再对102场景做10,000次区组bootstrap。效率失败只阻断多维效率SOTA，不取消独立准确率结果。

运行器/测试file SHA为`058c1072...c023/5224fc84...ae7`；PARROT公共捕获路径同时泛化为`套件/场景/种子`，避免7套件同名场景碰撞，更新SHA为`3cac917b...3e25`。GPU定向测试`20/20 PASS`，统一运行时、激活、external、PARROT、效率和目标审计联合回归`51/51 PASS`。真实入口仍返回`pending_selected_system_activation`，且formal效率protocol不存在；因此只把本地/隔离staging实现准备度从3/6提升到4/6，正式效率结果仍为0。

KRC权威快照更新为96/306 capture、576/1836 evaluation，90 primary+6 secondary、routing 0/96。Known F1最小/均值/最大为0.584658/0.611716/0.640981，0/96达到0.9；error-AUROC为0.669190/0.703697/0.743017，55/96达到0.7。32个三种子场景完整，其中20个具RRC诊断资格；source safety active最大0.002714且clean分类差0。progress canonical/file为`66ed04d9...1336/3667204c...0922`，bottleneck canonical/file为`5dfaaff3...99aa/571b71ae...62d`。

最新本地/隔离staging总账canonical/file为`4b11ca72...f4ad/ef71c6c1...bea0`，实现ready=4/6、complete=false，仍缺综合审计器和资源安全条件watcher。八项科学阻断保持不变：KRC和PUG未终态、Pairwise五指标严格支配仅3/7、两项污染门失败、外部恶意效果缺失、PARROT良性安全缺失、正式效率比较缺失。Pairwise仍只是provisional incumbent，`goal_achieved=false`，全面SOTA未授权。

### 8.15 下游工程闭包6/6但科学阻断不变

算法无关综合审计器和资源安全条件watcher已补齐。综合审计器同时绑定最终activation、其冻结的终选时目标总账、adapter design以及external/PARROT/efficiency三分支的protocol/summary/audit，逐项核验canonical与file SHA、算法身份和上下游manifest链。它要求终选时除三条下游未完成项外不存在其他科学阻断，因此当前绝对污染门、Pairwise--OpenDetect相对污染门或基线支配失败不能被三条下游阳性绕过；完整性通过与效果通过保持分离。

watcher在activation出现后先逐字节冻结其绑定的目标总账副本，再等待external输入、PARROT完整feature summary和306个KRC源捕获。只有训练/捕获进程为0、load1不超过逻辑CPU数的25%、`nvidia-smi`可观测且GPU计算进程为0连续3次，才设置exclusive环境门并按external、PARROT、efficiency、综合审计顺序执行；`--once --no-launch`只读检查永不启动任务。

综合审计器/测试SHA为`1413b5cf...54ba/0364b766...54ce`，watcher/测试SHA为`8d903871...0e8d/a9949387...8465`。新增11项GPU测试全部通过，selected-system联合回归为`62/62 PASS`；真实只读检查返回`waiting_for_final_selection_activation`。最新总账canonical/file为`5627bf59...563c/11bd411d...aea1`，selected-system实现ready=6/6、complete=true，但8项科学阻断、formal效果0、Pairwise provisional incumbent、`goal_achieved=false`和全面SOTA未授权均不变。

### 8.16 阶段评价纠偏与KRC 120项更新

已逐页核查`06_阶段性评价/CAEOS-EMTD评价-20260727.pdf`。评价关于方向合理、无泄漏协议较强、支持/冲突双路径有价值以及公开可复现性不足的判断予以吸收；其把Pairwise解释为已终选算法的判断与当前机器权威状态冲突，已明确纠正为`provisional incumbent`。Mondrian类条件支持路径已实现并有严格筛选/确认负向证据，不重新包装为新算法。

发布工程已解除`configs/*.json`、依赖锁和`reproducibility/**`的LFS误跟踪，并新增GPU精确环境快照以及Mal_TLS/Tor、HIKARI/Probing两数据集三路径最小复现包。`smoke`仅作执行完整性检查；`paper`固定seeds 7/19/31，对support、cauchy和nested路径执行18项剖面，不能替代strict-v4终选或全面SOTA。

KRC最新权威进度为120/306 capture、720/1836 evaluation，40个三种子场景完整。Known F1最小/均值/最大为0.584658/0.649359/0.890765，0/120达到0.9；error-AUROC为0.649182/0.704871/0.768064，69/120达到0.7。CIC-ToN-IoT已完成Backdoor、DDoS、DoS、Injection、MITM、Password、Ransomware和Scanning八个三种子场景，但源安全门全部失败，RRC诊断资格仍为20个场景。progress canonical/file为`b19303ca...ffd/ed8420e7...4d3`，bottleneck canonical/file为`41541d7b...27d/2c647731...f8a`。

刷新后的目标总账canonical/file为`9c9aa149...3a0/b645ec30...6c0`。selected-system实现仍为6/6，但8项科学阻断、formal效果0、Pairwise暂定参照、最优自有算法未终选、`goal_achieved=false`和全面SOTA未授权均不变。

### 8.17 KRC 124项跨套件诊断

KRC原子快照继续推进至124/306 capture、744/1836 evaluation，121 primary+3 secondary、routing 0/124。总体Known F1最小/均值/最大为0.584658/0.656225/0.974061，首次有1/124达到0.9；error-AUROC为0.649182/0.706525/0.908810，72/124达到0.7。41个三种子场景完整，RRC诊断资格仍为20个。progress canonical/file为`60e25048...386/43eccb53...31b`，bottleneck canonical/file为`1d725463...c19/8daf6d99...f2e`。

新增协议绑定的只读跨套件诊断器，只汇总known-validation证书，不读取外层未知测试标签，不授权算法选择、改门或部分结果终态聚合。diagnostic canonical/file为`1ca089da...b43/a059ab5a...364`，代码/测试SHA为`7e11e926...f99/16415eab...249`；GPU测试2/2通过，Windows/GPU canonical逐字节一致。

诊断揭示三种不同瓶颈：CIC-IoT2023为96 capture、Known F1均值0.611716、32个完整场景源安全0失败、20个RRC资格；CIC-ToN-IoT为27 capture、Known F1均值0.802709、9个完整场景全部源安全失败；CICIDS2017/Bot首个seed的Known F1为0.974061、error-AUROC为0.908810且源安全通过。该异质性支持保留KRC/RRC条件路由，而不支持用总体均值提前终选。

RRC接力静态复核确认输入注册、三种子证书、capture、evaluation、summary和独立audit均以`(suite, scenario)`为身份，并对7套件83个primary场景等套件聚合；未发现跨套件同名场景覆盖缺口，不修改冻结实现。最新目标总账canonical/file为`84c05d7c...d9f/49866baf...22f`，仍有8项科学阻断、`goal_achieved=false`，Pairwise仍为暂定参照。

### 8.18 RoNeTC域近邻缺口入账与KRC 128项更新

评价提出的域近邻基线已从旧39任务说明转为可执行strict-v4合同。经典主表继续固定MSP、Energy、OpenMax、kNN、ViM、Mahalanobis++和OpenDetect七项；域近邻主证据只保留OpenDetect与RoNeTC，ARPL/CADE降为旧39任务次级参考。RoNeTC协议固定7套件、102场景、seed 7和三类逐任务工件，canonical/file为`8ec67d1e...9b50/42b97f1b...bd38`；独立协议审计`passed=true`，canonical为`02f22fec...3917`。冻结时正式metrics、scores和provenance均为0，运行根目录不存在，因此只证明设计完备，不证明效果。

KRC同步推进至128/306 capture和768/1836 evaluation，42个三种子场景完整、43个场景身份已观测。Known F1最小/均值/最大为0.584658/0.665631/0.974061，5/128达到0.9；error-AUROC为0.649182/0.712975/0.945483，76/128达到0.7；RRC诊断资格增至21个完整场景。CICIDS2017现有5项捕获，其中Bot三种子完整，套件Known F1均值0.960579、error-AUROC均值0.912102，DDoS完成2/3种子。progress canonical/file为`b8b5de45...679/fbafd29e...832`，bottleneck canonical/file为`37e832a7...a10/d6f7bcfa...3cce`，跨套件诊断canonical/file为`a6b3fa80...301b/1e5a2694...0ff0`。

目标机器审计新增`domain_nearest_baseline_confirmed`要求，当前状态为`frozen_zero_result_not_executed`。最新总账canonical/file为`ea4c7b08...06ae/4cae7fc9...0590`，阻断数由8变为9；新增阻断是RoNeTC strict-v4 full102未完成。其余科学阻断、Pairwise暂定参照、`goal_achieved=false`和全面SOTA未授权均不变。

### 8.19 RoNeTC分析闭包与KRC 132项

RoNeTC协议在结果仍为0时完成最终缓存纠偏和重冻结。预检确认中间版所列7个postselection cache-dir不存在且NF-UNSW会错误回退到1500上限；当前命令改为直接使用OpenDetect baseline manifest绑定的7个CSV/sidecar，逐文件核验SHA并固定与full103相同的max-per-class。协议canonical/file为`38807dd2...e656/cb7085c8...c186`，协议审计canonical/file为`8fe711a6...1c96/e279f5f1...a25f`且通过。旧`8ec67d1e...9b50`和中间版`5629e2e8...9467`均只保留为历史快照。正式RoNeTC任务、summary、audit和completion仍全为0。

目标审计对未来终态增加了强绑定检查：102个metrics/scores/provenance必须齐全；summary必须绑定当前协议并报告102场景；独立audit必须重新复算且全部检查通过；completion必须同时绑定protocol、summary和audit的canonical/file SHA。完整性通过不代表RoNeTC优于OpenDetect，也不授权全面SOTA。相关GPU联合回归为`23/23 PASS`。

KRC原子进度同步为132/306 capture和792/1836 evaluation，44个三种子场景完整。Known F1最小/均值/最大为0.584658/0.674673/0.982441，9/132达到0.9；error-AUROC为0.649182/0.718886/0.945483，80/132达到0.7；RRC诊断资格由21增至23。CICIDS2017现有9项捕获，Bot、DDoS和DoS-GoldenEye三个场景均三种子完整，套件Known F1均值0.962111、error-AUROC均值0.910294，三场景均源安全通过且具RRC资格。

progress canonical/file为`3b3490db...8745/c8ddbbd9...7d2b`，bottleneck canonical/file为`69f86ce5...1107/f267d39e...13a1`，跨套件diagnostic canonical/file为`16290c24...444d/9d39ca48...8c01`。目标总账canonical/file更新为`8901b18e...b6da/3804db79...92fe`，仍为9项阻断、Pairwise暂定参照、`goal_achieved=false`和全面SOTA未授权。

### 8.20 KRC 140项独立审计更新

KRC watcher最新发布`progress_140.json`：140/306 capture、840/1836 evaluation，46个三种子场景完整、47个场景身份已观测。九项冻结不变量继续全部通过。Known F1最小/均值/最大为0.584658/0.691124/0.982441，17/140达到0.9；error-AUROC为0.649182/0.729795/0.945483，88/140达到0.7。

known-only bottleneck审计重新读取140项原子工件且`passes=true`，跨套件汇总状态为`valid_partial_diagnostic`。RRC诊断资格由23增至25个完整场景。CICIDS2017增至17项捕获、5个完整场景：Bot、DDoS、DoS-GoldenEye、DoS-Hulk和DoS-SlowHTTPTest均三种子完整、源安全通过并具RRC诊断资格；DoS-Slowloris当前为2/3种子。该局部改善仍不得替代306项终态或用于部分结果终选。

progress canonical/file为`b26abac7...1631/c8465fe0...4599`，bottleneck canonical/file为`b3c426de...e8c92/43888e44...4c46`，diagnostic canonical/file为`9e23d41c...4b47/8670d97f...2eb3`。目标总账canonical/file更新为`38b59393...565b/d5edf973...4adf`，阻断仍为9项、Pairwise仍为暂定参照、RoNeTC full102仍为零正式结果，`goal_achieved=false`和全面SOTA未授权。

### 8.21 终局编排纠偏、KRC可审计恢复与156项快照

对评价提出的“不要把工程准备误写为算法终选”和“补足可复现证据”进一步落实后，发现旧KRC downstream watcher会在KRC阳性后立即运行KRC专属external、PARROT和效率任务，而PUG/RRC终选尚未结束。这会使暂定候选抢跑最终算法的统一下游证据。现已把该watcher收敛为只生成canonical终态决策及handoff；RRC watcher继续只消费该决策，PUG跨套件watcher等待pilot，selected-system watcher等待最终activation并在每次轮询前刷新目标总账。当前各观察器均唯一存活且未启动下游效果任务。

部署统一运行时期间曾先同步`pairwise_runtime.py`、后同步其新增PUG常量依赖，导致KRC capture进程发生`PUG_RISK_NAME`导入错误。这是部署顺序事故，不是算法阴性。事故遗留154个只含同一ImportError日志的非完整capture目录；隔离脚本要求目录内只有`capture.log`、无`capture_manifest.json`且错误文本精确匹配，随后将其完整迁入`runs/strict_v4_krc_csr_confirmation_v1/quarantine/import_failure_20260727T0900Z`。隔离后原运行树为152个完整capture、0个半成品，隔离路径清单和日志表SHA分别为`eb0343cf...dd7`和`2a5452f7...e8a`，未删除任何历史工件。

同一冻结protocol和4-worker合同恢复后已越过故障点，watcher发布`progress_156.json`：156 capture/execution、936 evaluation、52个三种子场景完整，九项不变量全部通过。Known F1最小/均值/最大为0.584658/0.718316/0.982441，33/156达到0.9；error-AUROC为0.649182/0.747697/0.945483，104/156达到0.7。独立known-only audit `passes=true`，31个完整场景具RRC诊断资格。progress canonical/file为`41095b5e...9bdfad/69405166...f3d81`，bottleneck canonical/file为`03ea28c2...7ade8/02b62760...5ce31`，diagnostic canonical/file为`19a11c34...390f/ddb74283...c79fa`。

最新目标总账canonical/file为`57b0c0bd...81f6/f432f3fb...ff35`，仍有9项阻断：KRC部分进度、PUG fresh终态缺失、Pairwise仅严格五指标支配3/7经典基线、绝对污染门失败、相对OpenDetect污染门失败、RoNeTC full102未执行、external恶意、PARROT良性安全及同硬件效率未完成。双阳性直接锦标赛仍只有冻结设计，7项执行实现尚未补齐。Pairwise继续是provisional incumbent，`goal_achieved=false`，全面SOTA和最优自有算法均未授权。

### 8.22 95%/5%基本门与完整开放集门

用户要求现冻结为两层：基本预警门要求Alert Accuracy、Precision、Attack Recall和Known attack type Accuracy均`>=0.95`且Benign FPR`<0.05`；完整开放集门另要求Unknown attack alert Recall与Unknown label Recall均`>=0.95`。FPR95是未知高召回下的已知误拒，不等于Benign FPR。

102场景seed7只读复算的Alert Accuracy/Precision/Recall为`0.966649/0.981772/0.982693`，但Benign FPR为`0.268370`，基本门只通过`4/102`，完整门`0/102`。因此当前目标仍未完成。CICIDS2017分层概率诊断在2.5%与4%validation benign预算下通过基本门，4%预算结果为Alert Accuracy `0.972386`、Precision `0.995961`、Attack Recall `0.974113`、Benign FPR `0.046571`、Known type Accuracy `0.964400`；但Unknown alert/label Recall仅`0.910363/0.804053`，完整门失败。

本结果只说明单套件基本门可达。正式确认必须执行结果前冻结的fresh seeds `907/911/919`，且不能因seed7结果事后只选择CICIDS2017。核心协议canonical为`c8caaa110376851ddb44186362622ea08cc61e4eccd0168adccd65bb73ac9123`；RRC仍在运行并独立承担未知排序改进。最优自有算法、跨数据集95%/5%、RoNeTC完整基线、外部良性安全和全面SOTA均未完成，`goal_achieved=false`保持不变。

### 8.23 CPU后端诊断可达，GPU正式基本门与完整未知门均未完成

后端审计确认此前所谓“GPU正式确认”只是运行在GPU服务器，Pairwise和XGBoost训练实际使用CPU，因此正式结论撤回并统一降级为诊断。Pairwise分层概率在`907/911/919`上的均值Alert Accuracy/Precision/Recall、Benign FPR、Known type Accuracy和Unknown type Recall为`0.958889/0.996033/0.959528/0.045016/0.963626/0.729006`，但seed919 FPR为`0.055619`，故逐种子基本门失败。

开发seed7冻结的Empirical-Tail Hybrid CAEOS在三个CPU后端fresh诊断种子上全部通过基本门；总体Alert Accuracy/Precision/Recall、Benign FPR、Known type Accuracy、Unknown alert/type Recall为`0.967798/0.996061/0.969120/0.045492/0.960278/0.886051/0.794180`，诊断canonical为`a71c51e5...0b4`。它只能证明基本预警门显示可达性，不能升级为合规GPU正式通过；Unknown类型召回也仍低于95%。

同拆分CPU后端XGBoost总体Known type Accuracy为`0.992673`、FPR为`0.045048`，提示闭集分类可能不是主要剩余瓶颈；它没有Unknown标签头且seed919 FPR为`0.050095`，不能替代自有算法验收。服务器CPU后端失效分析把未知缺口分解为预警前漏检`0.155169`和已预警未标Unknown`0.115825`。

14个恶意细分类到7个恶意大类的映射已冻结。GPU开发v2协议`00136de7...8a77`已`7/7`完成、0失败，全部CUDA证据通过；completion `79e027e0...1202`记录A6000峰值利用率99%、显存2077 MiB。开发选择的Alert Accuracy/Recall、Benign FPR、Known family Accuracy、Unknown alert/family Recall为`0.901625/0.895768/0.041905/0.959176/0.765008/0.676494`，基本门和完整门均失败。阈值扩展135候选、双CUDA分类头630候选的基本门通过数均为0；后者最佳Alert Accuracy/Recall为`0.923686/0.921224`，仍不足95%。良性自编码器Botnet冒烟的Alert/Unknown alert Recall仅`0.205755/0.022889`，停止扩展。`953/967/971`未读取、fresh确认未启动。当前审计状态为：CPU后端单套件基本门仅显示可达；真实GPU攻击大类开发已闭合但未过门，GPU正式基本门、大类完整门、跨数据集确认、RRC终态、最优自有算法终选和全面SOTA均未完成，`goal_achieved=false`。

RRC已在`dns_spoofing/seed709` canonical边界从偏离协议的workers1切换到冻结合同workers4，新PID `465776`，状态canonical `9a348e6e...255ed`；独立复核为4个并发子任务，服务器load average `74.51`。该链仍为CPU后端，GPU 0%是当前算法后端属性，不是无任务。
