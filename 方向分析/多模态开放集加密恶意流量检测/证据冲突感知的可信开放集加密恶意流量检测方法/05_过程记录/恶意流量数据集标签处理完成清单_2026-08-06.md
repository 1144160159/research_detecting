# 恶意流量数据集标签处理完成清单

核查日期：2026-08-06  
远端核查主机：`root@10.0.5.103:25696`（`parkattack32`）  
远端核查时间：2026-08-06T07:36:29Z  
核查范围：`caeos_unified_multimodal_v5` 中承担恶意流量训练或外部攻击验证角色的8套数据集。

## 一、完成口径

本清单将“标签处理完成”分为三个层级，不能混用：

1. **严格流级完成**：冻结清单全部处理，保留流唯一匹配标签，冲突为0，未匹配流全部按批准原因删除并留痕，且 `formal_label_gate_passed=true`。
2. **捕获/成员级完成**：冻结清单全部处理，标签由PCAP路径或压缩包成员继承，且 `formal_dataset_gate_passed=true`；该层级不是官方逐流标签。
3. **源质量例外下可用**：所有来源均已读取或进入不可变的源质量例外，`source_quality_adjusted_gate_passed=true`，但严格 `formal_label_gate_passed=false`。

只存在脚本、SQLite、PID、队列 `complete` 字样或局部前缀审计，不单独构成完成证据。

## 二、严格流级标签处理已完成：5套

| 数据集 | 冻结清单 | 匹配/保留流 | 删除流 | 删除比例 | 冲突 | 正式门 |
|---|---:|---:|---:|---:|---:|---|
| CIC-BoT-IoT | 344/344 PCAP | 73,503,516 | 55,719 | 0.075747% | 0 | `formal_label_gate_passed=true` |
| CIC-ToN-IoT | 64/64 来源 | 19,677,154 | 15,196,068 | 43.575176% | 0 | `formal_label_gate_passed=true` |
| CICIDS2017 | 5/5 来源 | 1,681,228 | 520,824 | 23.651758% | 0 | `formal_label_gate_passed=true` |
| CICDDoS2019 | 965/965 来源 | 17,883,755 | 39,300,248 | 68.725948% | 0 | `formal_label_gate_passed=true` |
| Edge-IIoTset | 24/24 PCAP-CSV配对 | 7,597,160 | 31,095 | 0.407590% | 0 | `formal_label_gate_passed=true` |

上述5套可以表述为“已完成全部PCAP严格标签适配”。其中 ToN-IoT、CICIDS2017 和 CICDDoS2019 的删除比例较高，尤其 CICDDoS2019 达到68.73%，必须在论文数据流图、样本统计和局限性中披露，不能只报告保留样本。

## 三、全清单捕获/成员级标签处理已完成：2套

| 数据集 | 冻结清单 | 完整读取包数 | 保留流数 | 标签权威粒度 | 数据集门 |
|---|---:|---:|---:|---|---|
| CICIoT2023 | 309/309 | 4,493,106,426 | 115,500,168 | `capture_member_not_official_flow_label` | `formal_dataset_gate_passed=true` |
| CICIoT2022 | 1,152/1,152 | 119,626,824 | 2,653,788 | `capture_member_not_official_flow_label` | `formal_dataset_gate_passed=true` |

这2套已经完成全部PCAP或压缩包成员的完整性审计和标签继承，可以进入后续特征提取；但论文中必须写成“官方实验/捕获目录标签继承”，不能写成“官方逐流五元组与时间区间严格对齐”。跨数据集比较时应把标签粒度作为协议差异单列。

## 四、有条件完成、不能计入严格完成：1套

### DoHBrw2020

- 冻结可用来源：13,153/13,153 已处理。
- 其中1个官方PCAP物理截断，已按不可变源质量例外隔离；原始文件未修改。
- 匹配流787,117，剔除截断边界仍活跃的36,340条已匹配但不完整流后，保留完整匹配流750,777。
- 删除流836,695，占52.706126%；冲突0。
- `source_quality_adjusted_gate_passed=true`，但 `formal_label_gate_passed=false`。

因此 DoHBrw2020 可以表述为“标签扫描完成并在源质量例外协议下可用”，不能与前5套一起表述为“严格正式门通过”。正式实验若采用它，必须保留截断来源、排除规则和敏感性分析。

## 五、当前数量结论

- **标签处理完成：7套**，即5套严格流级完成，加2套捕获/成员级完成。
- **源质量例外下可用：1套**，即 DoHBrw2020。
- 当前8套恶意数据集标签任务均已退出运行态，远端 `queue.tsv` 显示7个 `complete` 和1个 `usable_with_source_quality_exceptions`。

不纳入本次计数的条目：ISCX Tor/NonTor、ISCX VPN/NonVPN主要承担加密隧道域偏移和良性误报验证；PARROT2025、CrossPlatform为良性安全验证；CICAPT-IIoT2024已按用户要求排除。

## 六、持久化、重建、适配完成结果

完成时间：2026-08-06。统一控制根目录为：

`/opt/data/private/wangwt/ParkAttackKE/CAEOS-EMTD/datasets/caeos_unified_multimodal_v5/_control/`

### 6.1 持久化

8套数据集均已在 `_control/label_indices/` 形成正式SQLite，共177,729,893条标签记录：

| 数据集ID | SQLite记录数 | 文件字节数 | 持久化状态 |
|---|---:|---:|---|
| `ciciot2023` | 309 | 208,896 | 新建捕获成员索引 |
| `ciciot2022` | 1,152 | 700,416 | 新建捕获成员索引 |
| `edge_iiotset` | 7,597,160 | 4,153,266,176 | 全哈希复用 |
| `cicids2017` | 2,830,743 | 1,748,307,968 | 最新索引发布，旧版改名保留 |
| `cic_bot_iot` | 73,367,604 | 31,380,746,240 | 分块修复后发布 |
| `cic_ton_iot` | 22,338,152 | 10,625,241,088 | 全哈希复用 |
| `cicddos2019` | 70,427,637 | 31,531,724,800 | 新发布 |
| `dohbrw2020` | 1,167,136 | 498,245,632 | 新发布 |

BoT原 `.partial` 与源SHA不一致；首次重写后NFS目标仍出现静默差异。持久化器改为512 MiB分块校验，最终复用40块、修复19块，并通过目标全文件回读。错误旧副本保留为 `cic_bot_iot.sqlite.partial.invalid.20260806T143018`，未作为正式输入。CICIDS2017旧版保留为 `cicids2017.sqlite.superseded.20260806T145453`。

### 6.2 重建

统一manifest已重建：

`_control/label_index_manifest.json`

- 文件SHA-256：`8fd9b1aabefbadb72738abfea22ea10cbcbb57add3cbaaac1f80039e21c39a28`
- `ready_dataset_count=8`
- 5套为 `strict_flow_label`
- 2套为 `full_capture_member_inventory`
- DoHBrw2020为 `source_quality_adjusted_flow_label`，仍明确保留 `formal_gate_passed=false`
- ISCX Tor/NonTor、ISCX VPN/NonVPN、PARROT2025、CrossPlatform未被误纳入本轮ready集合

严格审计summary继续保存在 `_control/audits/*strict*/summary.json`。CICIoT2022、CICIoT2023的summary、inventory和capture-label JSON，以及DoH的summary和源质量策略，均已从临时位置复制到：

`_control/audits/label_delivery_20260806/`

### 6.3 适配

CICIoT2022和CICIoT2023已由JSON捕获标签转换为统一的 `caeos_label_alignment_sqlite_v1`：

- CICIoT2022严格使用已验收inventory中的1,152个成员，不纳入源manifest中额外的 `CSV files.tar.gz` 附带PCAP。
- CICIoT2023严格使用309个冻结PCAP。
- 1,461个成员全部完成路径标签与SQLite权威标签交叉核验。
- 两套索引的真实解析状态均为 `aligned_unique_capture`。
- CICIoT2022攻击路径可恢复 `Flood-HTTP/TCP/UDP` 和 `RTSP Brute Force-Hydra/Nmap`；CICIoT2023的 `Benign_Final` 统一映射为 `BenignTraffic`。

## 七、验证证据

远端测试：相关标签交付、成员适配、manifest和统一预处理回归共26项，结果 `26 passed`。

最终验证报告：

- `_control/audits/label_delivery_20260806/validation.full_hash.json`：8/8 SQLite当前文件全SHA-256通过。
- `_control/audits/label_delivery_20260806/validation.adaptation.json`：8/8元数据链通过，CICIoT成员路径交叉核验309+1,152条通过。

验证链为：统一manifest -> index audit SHA -> SQLite路径/记录数 -> SQLite metadata -> 当前文件全SHA-256。仅有PID、文件存在、大小相同或历史summary均不替代该验证链。

## 八、最终判断

本轮要求的三个标签工程步骤已经完成：8套恶意流量数据集均已持久化，统一label index manifest已重建，CICIoT捕获成员与DoH源质量例外均已适配到统一入口。当前可以启动后续统一特征提取。

边界仍需保持：本结论是“标签交付入口完成”，不等于特征CSV已经生成，也不等于模型实验已完成；DoHBrw2020仍不能表述为严格流级正式门通过。
