# 标签处理进度核查

首次核查时间：2026-08-05 04:11（GPU 服务器时区）  
续处理复核：2026-08-05 05:20 后（SLL 修复已部署）；二次恢复复核：合法空 PCAP 修复后推进至 1,707/13,154  
核查主机：`root@10.0.5.103:25696`（`parkattack32`）  
远端代码：`/opt/data/private/wangwt/ParkAttackKE/CAEOS-EMTD/preprocessing/caeos_unified_multimodal_v5_20260802_r14`  
队列运行目录：`/tmp/caeos-eight-dataset-strict-queue-r17`

## 一、核查口径

本记录不以 `queue.tsv`、PID 存在或 PCAP 扫描结束作为“标签处理完成”的充分条件。数据集只有同时满足以下条件才记为正式完成：

1. 冻结清单内所有 PCAP、压缩包成员或 PCAP/CSV 配对均已完整读取。
2. 所有保留流唯一匹配官方标签。
3. 冲突流为 0，或者有预先声明且可审计的冲突处理规则。
4. 所有未匹配流均进入批准的删除原因，并记录数量和比例。
5. `formal_label_gate_passed=true`。

## 二、数据集总进度

| 数据集 | 清单处理进度 | 正式门 | 当前状态 | 核心证据 |
|---|---:|---|---|---|
| CIC-BoT-IoT | 344/344 PCAP（100%） | **通过** | 时间不重叠处置已按官方标签政策重算 | 73,503,516 匹配流；55,719 个未匹配流全部按批准原因删除并留痕 |
| CIC-ToN-IoT | 64/64 来源（100%） | **通过** | 正式标签适配完成 | 19,677,154 匹配流；15,196,068 流按批准原因删除；冲突 0 |
| CICIDS2017 | 5/5 来源（100%） | **通过** | 正式标签适配完成 | 1,681,228 匹配流；520,824 流按批准原因删除；冲突 0 |
| CICDDoS2019 | 965/965 来源（100%） | **通过** | 正式标签适配完成，但删除比例需在论文中披露 | 17,883,755 匹配流；39,300,248 流删除，占 68.73%；冲突 0 |
| DoHBrw2020 | 1,707/13,154 来源（12.98%，二次恢复快照） | **未通过** | 修复后持续运行 | SLL/IPv6 误判与合法空 PCAP 进度崩溃均已修复；1 个截断 PCAP `NeedData` 继续如实留痕 |
| CICIoT2023 | 73/309 PCAP（23.62%，复核快照） | **未通过** | 正在运行 | 当前为 capture/member 级标签权威，不是官方逐流标签；尚未覆盖完整清单 |
| CICIoT2022 | 19/1,152 PCAP/成员（1.65%） | **未通过** | 正在运行 | 当前为 capture/member 级标签权威，不是官方逐流标签；正在处理一个大 PCAP |
| Edge-IIoTset | 24/24 PCAP-CSV 配对（100%） | **通过** | 正式标签适配完成 | 7,597,160 匹配流；31,095 删除流；完整标签索引通过只读校验 |

按续处理复核时的正式门统计：**8 个数据集中 5 个通过，3 个正在处理且未通过。**

## 三、当前进程实况

二次恢复复核时真实活跃的标签任务有三条：

| 数据集 | 审计 PID | PCAP 读取进程 | 已运行时间 | 核查时文件 |
|---|---:|---:|---:|---|
| CICIoT2023 | 124459 | 动态 `tshark` 子进程 | 持续运行 | 已由 46/309 推进至 73/309 |
| CICIoT2022 | 124481 | `tshark` PID 124796 | 约 3 小时 38 分 | `2021_12_08_Active.pcap` |
| DoHBrw2020 | 130376 | Python/`dpkt` 逐来源审计 | 二次恢复后持续运行 | 已由 369/13,154 推进至 1,707/13,154 |

旧总队列 PID 124208 已在确认只有僵尸子进程、且不属于 CIC 活跃进程树后退役。新的只读状态监视器每 30 秒根据真实进程原子刷新 `queue.tsv/status`；复核时 DoHBrw2020、CICIoT2022、CICIoT2023 为 `running`，其余五套为 `complete`。

## 四、逐数据集核查结论

### 4.1 CIC-BoT-IoT

- 全部 344 个 PCAP 已完整读取，数据源未被修改。
- 总流数 73,559,235；匹配 73,503,516；未匹配 55,719。
- 36,439 个流进入批准删除原因：五元组不在官方标签中 36,433，官方 TCP/UDP 标签范围外协议 6。
- 另外 19,280 个流属于“五元组存在但时间不重叠”，已按用户确定的“以官方标签为准”政策纳入批准删除原因。
- 该原因占全部流 0.026210%，涉及 23,200 个包（0.004220%）和 3,563,704 字节（0.005427%）。
- 三类原因合计删除 55,719 流，占全部流 0.075747%；冲突为 0，`effective_coverage_fraction=1.0`，`formal_label_gate_passed=true`。
- 旧 summary 和逐 PCAP 审计已备份，未删除。

### 4.2 CIC-ToN-IoT

- 64/64 个冻结 PCAP/压缩包成员完整读取。
- 总流数 34,873,222；匹配 19,677,154；删除 15,196,068，占 43.58%。
- 删除原因包括五元组不在官方标签、时间不重叠和官方 TCP/UDP 标签范围外协议，均已被批准。
- 冲突流为 0，`formal_label_gate_passed=true`。
- 队列目录中的早期 `exit_code=4` 是陈旧失败记录；最终 summary 已达到正式门，不能再用旧退出码否定当前产物。

### 4.3 CICIDS2017

- 5/5 个冻结来源完整读取。
- 总流数 2,202,052；匹配 1,681,228；删除 520,824，占 23.65%。
- 删除中 515,398 个为时间不重叠，按既定政策拒绝并留痕。
- `formal_label_gate_passed=true`。

### 4.4 CICDDoS2019

- 965/965 个来源完整读取。
- 总流数 57,184,003；匹配 17,883,755；删除 39,300,248，占 68.73%。
- 39,293,965 个删除流为五元组存在但时间不重叠；按官方标签优先政策拒绝。
- `formal_label_gate_passed=true`，但高删除比例会显著改变数据分布，必须在数据章节、数据流图和局限性中单独披露，不能只写“标签适配完成”。

### 4.5 DoHBrw2020

- 冻结来源 13,154 个，首次核查只完成 369 个；修复后独立复核已推进至 1,675 个（12.73%）。
- 一个 PCAP 在记录头或包体中截断，`dpkt` 报 `NeedData: got 5, 16 needed at least`。
- 后续重试表面在 IPv6 扩展头解析处触发：`AttributeError: 'IP6HopOptsHeader' object has no attribute 'frag_off'`。进一步核查发现真实根因是 Linux SLL 封装的 IPv4/TCP 被旧代码先按 Ethernet 误判成 IPv6。
- 已加入 SLL 优先识别和 IPv6 扩展链回退。第 370 来源重算为 5,210/5,210 包、1 条 TCP 流、覆盖率 1.0、零排除、无读取错误。
- 原 1-369 审计保留；错误修复前产生的 370-1567 共 1,198 份审计移入备份目录，未删除。已知截断源仍标 `complete_pcap_read=false`，不会伪装完整。
- 第 1,680 来源是合法的 24 字节 header-only PCAP，审计报告已正确记录 0 流，但旧进度输出硬取不存在的 `counters["flows"]` 而触发 `KeyError`。现改为缺省输出 `flows=0`，不改变读取、匹配或删除规则；保留 1-1,680 审计后从下一来源续跑。

### 4.6 CICIoT2023

- 冻结清单 309 个 PCAP，首次核查完成 46 个；续处理复核已到 73 个（23.62%）。
- 已完整读取 708,839,676 个包，保留 9,414,585 个流。
- 当前权威粒度为 `capture_member_not_official_flow_label`，即按捕获文件/成员标签，而不是官方逐流索引。
- `exact_inventory_source_id_coverage=false`、`formal_dataset_gate_passed=false`，必须等待全部 309 个来源通过完整读取审计。

### 4.7 CICIoT2022

- 冻结清单 1,152 个 PCAP/成员，完成 19 个（1.65%），第 20 个大 PCAP 正在处理。
- 已完整读取 38,492,343 个包，保留 792,439 个流。
- 当前权威粒度同样为 capture/member 级，不是官方逐流标签。
- 当前大 PCAP 已持续处理数小时，不能用前 19 个小文件的平均速度外推总完成时间。

### 4.8 Edge-IIoTset

- 24/24 个 PCAP-官方 CSV 配对全部完成严格对齐。
- 匹配 7,597,160 流，删除 31,095 流，冲突 0。
- 全量 SQLite 标签索引记录数 7,597,160，quick check、记录数、schema 和数据集 ID 均通过。
- `formal_label_gate_passed=true`，但 summary 明确 `feature_extraction_started=false`：这只代表标签处理完成，不代表特征预处理已经开始或完成。

## 五、当前阻塞与整改优先级

1. **P0：不要把队列状态当正式完成状态。** 统一以各数据集 summary 的 `formal_label_gate_passed` 为准；状态监视器只负责呈现进程实况。
2. **已完成：修复 DoHBrw2020 的 SLL/IPv6 误判并从第 370 个来源续跑。** 现有任务不得停止。
3. **已完成：修正 BoT-IoT 的时间不重叠政策并重算正式门。** 删除原因、规则和比例已留痕。
4. **已完成：让队列视图与真实进程一致。** 监视器只读探测进程，不修改正式完成判定。
5. **P1：将正式索引、summary、清单和 SHA-256 从 `/tmp` 发布到 CAEOS-EMTD 持久目录。** `/tmp` 产物不应作为最终实验基础。
6. **P1：CICDDoS2019 和 ToN-IoT 的高删除比例进入论文披露与敏感性实验。** 正式门通过不代表数据偏差风险消失。
7. **P2：CICIoT2022/2023 完成后再进入特征提取。** 当前 capture/member 标签粒度必须在跨数据集实验中作为标签精度限制披露。

## 六、结论

当前标签处理仍未整体完成。严格口径下，BoT-IoT、ToN-IoT、CICIDS2017、CICDDoS2019、Edge-IIoTset 已通过；DoHBrw2020 已修复并持续运行；CICIoT2022 和 CICIoT2023 未中断。现阶段仍不能把 8 个数据集全部作为统一预处理基础数据集投入正式实验。

## 七、修复验证

- 本地与远端的解析器、全来源审计器、测试和两个监督/监视脚本已核对哈希一致。
- 标签子任务报告远端定向测试 `26 passed`。
- 主代理独立在本地复跑解析器、全来源审计和读取失败相关测试：`24 passed in 3.76s`。
- 合法空 PCAP 进度修复新增回归测试；主代理使用独立本地 Python 运行 `8 passed in 1.50s`，远端同步后运行 `8 passed in 0.30s`。
- 本地验证只证明代码路径，不替代 GPU 服务器上的正式全量处理；正式进度继续以远端 summary 为准。
