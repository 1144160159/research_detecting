# 变更记录

## v0.9-darpa-trace-complete — 2026-07-17

- 完成 `ta1-trace-e3-official.json.tar.gz` 规范落盘，大小 `37,072,393,089 B`；gzip 全流与 TAR 结构检查通过，SHA-256 为 `0f00146c48fd7fb90052a39993c8f1333bf116ce5d25857c5f497f8bfbc6654c`；
- 停止 TRACE official 恢复进程，清除约 7.8 GB 分块、1.54 GB 历史 partial、状态和日志，相关残留记录为 0；
- DARPA E3 全 JSON 扩展清单由 `8/9` 更新为 `9/9`、`43,028,712,331 / 43,028,712,331 B`。

## v0.8-provcon-full-collection — 2026-07-16

- 在 GPU 全量克隆 `NUS-Curiosity/provcon`，固定提交 `57e38a58b3d62d295a20259f7ab49a616a112419`，覆盖 APT1、APT17、APT29、APT32、APT32-variant 与 APT41；
- 核验 Git 跟踪 74 文件、仓库 `1,225,202,221 B`；24 个日志 ZIP、24 个 PCAPNG、19 个溯源 DOT 和 6 个攻击标注 CSV 均已落盘；
- Git fsck、LFS fsck、`24/24` ZIP 结构测试与 `24/24` PCAPNG 魔数检查通过；确认仓库未使用 LFS，不存在指针占位；新增带 Git 克隆/LFS 自动重试的 `run_provcon_full_collection.sh`。

## v0.7-optc-collection-start — 2026-07-16

- 从法国国家研究数据仓库 DOI `10.57745/UXCWOC` 取得纠错版 OpTC 的 10 个逐日 TAR 清单，总计 `939,321,856,000 B`，登记逐文件 ID、官方 MD5 和直接访问 URL；
- 在 GPU 启动 2019-09-23 至 2019-09-25 三个攻击评估日的并行断点续传，共 `299,968,993,280 B`，每文件 4 连接，并配置 MD5、SHA-256 双重验收；
- 新增 `run_optc_corrected_full_queue.sh`，攻击期任务结束后自动复用已完成文件并补齐全部日期；新增带重试退避的 `enumerate_gdrive_folder.py`，完成 Google Drive 原版 `1,277` 文件枚举（ecar 366、ecar-bro 156、bro 751）；新增并启动 `run_optc_original_gdrive_queue.sh`，自动断点下载全目录并在失败时每小时重试。

## v0.6-darpa-local-ingest — 2026-07-16

- 将本地临时完成的 THEIA-6r、CADETS-1、THEIA-1r、TRACE-1 四个官方 JSON 归档上传到 GPU 临时接收目录，总计 `4,945,569,740 B`；远端 SHA-256 与上传前逐项一致后原子移动到 `apt_public/darpa_tc/raw/json`；
- 写入 `raw/json/local_upload_20260716.sha256` 验收清单；停止四个旧续传进程并清除对应分块、状态和日志，不影响 TRACE official JSON 独立续传；
- DARPA E3 混合表示核心由 `3/4` 更新为 `4/4`，全 JSON 扩展清单由 `4/9` 更新为 `8/9`，当前仅剩 TRACE official JSON 未完成。

## v0.5-darpa-quota-recovery — 2026-07-16

- 针对一次性反向隧道退出造成的数轮空转，新增自动重连 PowerShell 守护脚本和 Windows 登录计划任务；守护 SSH 使用 `-F NUL` 隔离用户配置中的 `RemoteForward 9999`。通过主动杀死 SSH PID `38984` 验证约 15 秒后新 PID `35920` 自动建立、计划任务保持运行且远端 `9998` 恢复监听；
- 在隧道恢复后立即重启五个远端配额恢复进程，本轮均得到可归因的官方 `google_drive_quota_exceeded`，排除代理连接错误；
- 实时复核 `apt_public` 为约 `289 GB`，共享盘剩余约 `7.2 TB`；确认 AInception、DEDALE、AIT-LDS V2.1、CAM-LDS 等首批队列已全部完成，而旧 AIT-LDS V2.0 继续保持 `paused_superseded`；
- 修复 DARPA 恢复进程所需的远端 `127.0.0.1:9998` SOCKS 反向通路，复核 THEIA-6r 当前严格有效块为 `630,621,875 B`；
- 新增并同步 CADETS-1、THEIA-1r、TRACE-1 JSON、TRACE official JSON 四个单文件恢复 manifest；与 THEIA-6r 一起启动五个互不覆盖的每小时配额门控进程；
- 五个源的即时探测均由“代理连接拒绝”纠正为可归因的官方 `google_drive_quota_exceeded`；下载器仅在 Range、Content-Range 和字节数全部匹配时接纳分块；
- 保持本地 `source` 只存采集代码和轻量 JSON manifest，所有原始包、partial、分块、日志和哈希状态仍只在 GPU 服务器。

## v0.4-public-dataset-expansion — 2026-07-15

- 完成首批六组、Unraveled、PWNJUTSU、SAGA、Linux-APT、Windows-APT、Sandworm、CIC 完整版、OTRF APT29、NODLINK、APTsDataset、StreamSpot 和 DAPT2020 的远端落盘与相应完整性校验；
- 经真实授权登记取得 CICAPT-IIoT2024 全部 `43/43` 文件；逐项 SHA-256 与 PCAP 魔数审计通过，并以符号链接复用历史 9,809,174,913 B NetworkData CSV；
- 完成 AutoLabel 两个多源场景、CAM-LDS 场景 3 的 6 个 PCAP ZIP，以及 Splunk APT simulations+FIN7 的 10 个 LFS 日志；继续采集 DARPA E3 四包核心集（CADETS 两包 JSON、THEIA-6r JSON、TRACE-1 官方 binary），扩展 9 包 JSON 延后；
- 完成 MAGIC 固定提交发布的 CADETS/THEIA/TRACE/StreamSpot/Wget 五套预处理图远端落盘、大小核对、SHA-256 与 ZIP 测试；修正多粒度重叠分片的进度统计为区间并集，THEIA-6r 官方 JSON 原始包继续后台续传；
- 将 SimuLand 明确登记为场景生成器而非预采集数据，将 LADEMU 登记为“暂无当前官方公开载荷”，禁止以第三方镜像冒充；
- 把 OTRF 从 P2 调整为 P0 场景库候选，把 StreamSpot/DAPT2020 限定为基线或历史消融，并新增许可审计风险；
- 更新 `public_datasets.yaml` 至 v4，纠正 Unraveled 完成清单为 `174` 个 processed 对象；本地仍只保存代码、配置、哈希和轻量状态，所有 payload 只在 GPU。

## v0.3-public-dataset-collection — 2026-07-15

- 在 GPU 共享数据根建立 `datasets/apt_public`，并以项目 `datasets/public` 软链接接入；
- 启动 DEDALE V2、AIT-LDS V2.1/AIT-NDS/AIT-ADS、CAM-LDS 和 AInception 的断点续传与双哈希校验队列；
- 完成 AIT-NDS、AIT-ADS、OpTC/DARPA 官方元数据仓库以及 SOCBED 工具/评估数据的首轮落盘；
- 暂停旧 AIT-LDS V2.0 完整 PCAP，保留可恢复控制文件，不把部分下载记为完成；
- 新增 Zenodo/Dataverse GPU 采集器、状态汇总脚本、公开数据准入记录和远端路径配置；
- 记录 Google Drive 配额/连通性阻塞，OpTC/DARPA 原始大包不提前声明已获得。

## v0.2-research-design — 2026-07-15

- 将主问题从泛化“三源融合”收敛为“失配/缺源下的不确定性跨源对齐+最小充分证据链”；
- 完成 12 类自动召回、11 个环节、53 篇代表论文的组件候选复核；
- 明确三个预期贡献、14 个创意候选、ChainBreak/连接点保真压缩两个备选；
- 建立 H1-H5、E0-E8、效应阈值、失败解释和两周 Go/No-Go；
- 确认 Strong-Truth 18-24 次攻击运行是第一数据门，LANL 11GB/181GB 分别承担弱标签与规模；
- 建立远端 Strong-Truth 目录和 manifest/JSON Schema/标注模板；
- 将本地 `source/EACR-APT` 固定为代码-only，数据、模型和大结果只存 GPU 服务器；
- 将 TeRed、LLM、GAN、大型异构 GNN、在线/联邦/开放集等降为基线、后续或排除项。

## v0.1-planning — 2026-07-15

- 建立论文子方向独立工作区；
- 确定三源时序证据图与阶段约束攻击链重构主线；
- 建立总览、方案、实验、报告、数据运行、文献证据、论文写作、过程记录与结果注册表；
- 从 858 篇本地语料定位 30 篇核心/桥接文献；
- 登记 KRYSTAL、TeRed、Euler、eaGle、TraceCluster、Open-CyKG 等可复用资产及缺口；
- 明确当前只有规划/资产证据，没有联合模型结果。

后续每次改变研究问题、数据来源、主指标、成功阈值或论文主张，都必须新增版本条目并链接对应决策日志。
