# 目标完成度审计

更新日期：2026-07-24。

| 目标 | 状态 | 当前证据或缺口 |
|---|---|---|
| 本地代码仓与禁入规则 | 已完成 | `source/HFT-MGBS` 只保留代码、配置、测试和文档；策略检查未发现数据、模型或运行结果 |
| 多粒度特征抽取 | 已完成工程基线 | packet/flow/window/deep 四级；双向流、线速载荷长度、超时与 FIN/RST 滚动语义已有测试 |
| 自适应预算调度 | 已完成工程基线 | 成本/效用 EMA、压力收缩、关键流预留、配置预算不可扩张 |
| 执行期真实预算审计 | 已完成 | 估算与实测成本分离；失败 deep 尝试也计费；按实际执行 tier 重算关键流覆盖 |
| 回退与恢复 | 已完成同候选三次验证 | 同一 `batch512/budget5000/safety0.50` 管线注入 deep 故障，在熔断开启时处理真实 Tinba PCAP，再由探针恢复；三次预算超限 0、关键流覆盖 100%，恢复最坏约 0.270 秒 |
| 硬约束先于 Pareto | 已完成 | 预算、覆盖、资源、重复次数及已冻结时延约束先过滤；违规候选不会进入前沿 |
| GPU 代码镜像与环境验证 | 已完成 | 直接 SSH 到 `10.0.5.103:25450`；远端 py3.9 通过 83 项测试和本地存储策略检查 |
| 物理 NIC 线上证据合同 | 已完成代码门禁 | `validate_live_evidence.py` 要求物理 NIC 可见、XDP/AF_PACKET 驱动来源、目标负载、五段计数对账、端到端时间点、资源、回退和冻结阈值；布尔声明不能替代证据 |
| PCAP 正确性参照 | 部分完成 | HFT 与 NTL 在可比较 Geodo 流上的包数、载荷字节、时长和全局 flags 一致；NTL 本身存在解析错误和方向/IAT 缺陷，不能作为最终 oracle |
| 三次离线性能重复 | 已完成预筛 | Tinba、batch 512、预算 5000us；normal/fallback 的实测预算超限均为 0、关键流覆盖均为 100% |
| 执行期预算守卫有效性 | 已完成 | 无守卫候选每次出现最多 2 个实测超限批次，门禁正确淘汰；守卫后 6/6 重复无超限，optional 最大约 4.50ms |
| 分组质量协议 | 已完成预筛 | 18 个 USTC-TFC2016 PCAP 按完整 capture 分组，三次独立抽取，30414 条流；禁止同 capture 跨训练/测试 |
| 联合离线 Pareto | 已完成预筛 | fallback 在吞吐/P99占优，normal 在最差 macro-F1 占优，两者均为非支配候选 |
| 目标负载与 SLA 阈值冻结 | 未完成 | 目标 Gbps/Mpps、P99、P999、排队、恢复时间和最低 macro-F1 尚未由业务验收方冻结 |
| 真实 NIC 丢包与线上端到端时延 | 未完成 | 当前容器只可见 10Gbps veth，缺少物理 NIC 视图与 CAP_NET_ADMIN；离线批次 P99 不等价于线上端到端 P99 |
| 独立/时间外质量留出集 | 已完成独立预筛 | 18 个 USTC capture 训练、6 个 UNSW 分片留出，27 个输入条目共 10,040,558,320 字节已冻结哈希；三次提取。fallback 最差 macro-F1 0.4157，normal 0.3265，事件覆盖 0.6682，跨域泛化仍弱 |
| 24/72 小时长稳与生产回滚演练 | 未完成 | 尚缺线上负载、告警、队列、故障注入、恢复和回滚的同版本长稳证据 |
| 最终 Pareto Champion | 禁止输出 | 证据门仍缺真实目标负载、live replay、物理 NIC drop、端到端 P99/P999、24/72h 长稳和冻结阈值；跨域质量也需在冻结门限下重新判定 |

当前结论只能称为“四源离线联合 Pareto 预筛已形成，工程硬门禁已可执行”。normal/fallback 是同一部署配置的两个运行模式，不应被误写为两个最终候选；生产最优方案尚未验收。
