# 论文补充后处理 SOP

适用场景：每次向 `paper/` 文件夹新增 PDF 后，按本流程完成文件规范化、文献库更新、综合分析并入和方向报告同步。

当前口径：`paper/` 是唯一 PDF 原始库；专题目录只保存分析、索引、证据、报告和论文初稿，不复制 PDF。

## 1. 确认新增范围

补充 PDF 后，首先确认新增数量和新增文件名。

```powershell
(Get-ChildItem -LiteralPath 'paper' -File -Filter '*.pdf' | Measure-Object).Count
Get-ChildItem -LiteralPath 'paper' -File -Filter '*.pdf' |
  Sort-Object LastWriteTime -Descending |
  Select-Object -First 30 Name,LastWriteTime,Length
```

需要记录：

1. 本次新增几篇。
2. 新增文件是否都能打开。
3. 是否有重复下载、预印本与正式版重复、文件损坏或空 PDF。
4. 新增论文分别服务哪个方向，例如“可信开放集检测”“协议语义预训练”“DA-FDIDS”等。

## 2. 规范 PDF 文件名

优先用 DOI 命名。建议规则：

1. 有 DOI 的 PDF：`10.xxxx_xxxx.pdf`，将 DOI 中的 `/` 替换为 `_`。
2. arXiv 论文：优先写作 `10.48550_arXiv.xxxx.xxxxx.pdf`。
3. 没有 DOI 的报告、白皮书、技术手册：保留清晰英文题名，但必须在 `文献.md` 中注明无 DOI。
4. 若存在重复 DOI，先判断是否同一论文不同版本；不要直接加 `_dup`，应保留正式版，预印本只在备注中说明。

每次重命名前先做 DOI 提取和人工核验：

```powershell
pdftotext "paper/xxx.pdf" -
pdfinfo "paper/xxx.pdf"
```

命名完成后检查是否仍有明显非规范文件名。

## 3. 补全元数据

每篇新增论文至少要整理以下字段：

| 字段 | 要求 |
|---|---|
| 序号 | 接续 `文献.md` 当前最后编号 |
| 标题 | 与原文首页或出版页一致 |
| 作者 | IEEE 引用格式需要首作者与作者列表 |
| 年份 | 以正式出版年份为准；预印本单独标注 |
| 来源 | 期刊、会议、arXiv、白皮书等 |
| 卷期页码 | 有则补齐 |
| DOI/URL | DOI 优先；无 DOI 则给稳定 URL |
| PDF 文件名 | 必须与 `paper/` 中实际文件一致 |
| 方向标签 | 对应五个方向或专题子方向 |
| 相关性 | 强相关 / 中相关 / 弱相关 |
| 创新点标签 | 例如可信检测、开放集、OOD、证据学习、校准等 |

## 4. 更新参考文献文件

新增论文后必须同步更新两个根目录文件：

1. `文献.md`
2. `文献引用.txt`

更新要求：

1. 顶部总数从旧数量更新到新数量。
2. 生成日期更新为当天日期。
3. 新增条目按编号追加，不要打乱旧编号。
4. `文献.md` 使用完整 IEEE 格式，并包含 PDF 链接和 DOI 链接。
5. `文献引用.txt` 保持纯文本引用格式，编号与 `文献.md` 完全一致。
6. 同一 DOI 不应重复进入正式编号；如确需保留多个版本，在条目备注中说明。

## 5. 更新综合分析索引

新增论文不只是加入参考文献，还要进入综合分析体系。需要同步检查并更新：

| 文件 | 操作 |
|---|---|
| `综合分析/论文分析总表.csv` | 新增论文元数据、相关性、方向分类、创新点标签、代码状态 |
| `综合分析/01_大类归类统计.md` | 更新大类数量和代表论文 |
| `综合分析/02_创新点归类分析.md` | 更新创新点类别统计 |
| `综合分析/03_论文相关性分析.md` | 将强相关/中相关补充到对应方向 |
| `综合分析/05_逐篇中文解析.md` | 添加新增论文的中文解析入口 |
| `综合分析/07_代码对照总表.md` | 如果论文有代码，补代码仓库与本地路径 |
| `综合分析/09_GPT5.5逐篇精读索引.md` | 增加精读状态和精读文件链接 |

如果只是临时补充少量理论根论文，也至少要更新 `论文分析总表.csv`、`文献.md`、`文献引用.txt` 和相关方向报告。

## 6. 生成逐篇分析

每篇新增论文都应至少形成一份中文解析。强相关论文建议生成完整精读。

推荐内容结构：

1. 基本信息：题名、年份、来源、DOI、本地 PDF。
2. 中文摘要与核心结论。
3. 解决的问题。
4. 方法和技术路线。
5. 创新点。
6. 实验设计和指标。
7. 局限性。
8. 与本项目的关系。
9. 可复现线索和代码状态。
10. 建议在论文初稿中引用的位置。

输出路径建议：

```text
综合分析/GPT5.5逐篇精读/[编号]_[英文题名截断].md
```

如果只是中相关或理论基础论文，也可先写短版中文解析，后续再升级为精读。

## 7. 并入方向分析

每次新增论文都要判断是否影响某个方向已有结论。对于“多模态开放集加密恶意流量检测”，重点检查：

| 文件 | 更新方式 |
|---|---|
| `方向分析/多模态开放集加密恶意流量检测/可信开放集检测方向整体方案分析.md` | 将外部检索来源改为本地已补论文，补充“本地编号”和作用 |
| `方向分析/多模态开放集加密恶意流量检测/850篇论文_新写作方向与创新点挖掘.md` | 若总数变化，建议新建或改名为新版分析；补新增论文对创新点的影响 |
| `方向分析/多模态开放集加密恶意流量检测/论文初稿_MM-OpenEMTD_多模态开放集加密恶意流量检测.md` | 更新相关工作、方法依据、实验基线和参考文献编号 |
| 方向 README | 如方向入口或核心文献清单变化，应同步更新 |

原则：新增论文不能只躺在 `paper/` 中，必须进入对应方向的“证据链”。

## 8. 更新论文初稿

对强相关论文，需要明确进入初稿哪个位置：

1. 引言：支撑研究问题和真实痛点。
2. 相关工作：按类别加入综述。
3. 方法：支撑 evidence、DS fusion、energy、prototype、calibration 等模块。
4. 实验：作为基线、指标或数据划分依据。
5. 讨论：支撑局限性、未来工作和落地风险。

对于可信开放集检测方向，本次补充论文建议对应关系：

| 类型 | 应进入初稿的位置 |
|---|---|
| Evidential Deep Learning | 方法章节：Dirichlet evidence 与 uncertainty |
| OpenMax | 相关工作：开放集深度网络经典方法 |
| Energy OOD | 方法和实验：能量分数 Unknown 风险 |
| Mahalanobis OOD | 方法和基线：原型/距离式 OOD 检测 |
| Calibration | 实验指标：ECE、温度缩放、风险校准 |
| FOSS / M3S-UPD / New Class Detection | 相关工作：最新开放集网络流量检测 |

## 9. 更新计数和文件名口径

如果 `paper/` 总数变化，所有带数量的文件名或标题都要检查：

1. `文献.md` 总计。
2. `文献引用.txt` 总计。
3. `综合分析` 中的统计报告。
4. 方向报告中类似“850篇论文”的标题和正文。
5. README 中关于论文数量的描述。

如果旧报告确实只基于旧数量，可以保留旧名，但要新增一段说明：

```text
注：本文基于当时 850 篇论文生成；后续补充论文已在 xxx 文件中并入。
```

更推荐做法是新建“增量并入记录”，避免频繁改动旧分析。

## 10. 记录补充并入日志

每次补充后建议维护一份日志，可放在根目录或对应方向目录：

```text
论文补充并入记录.md
```

每次记录：

1. 日期。
2. 新增文件数。
3. 新增论文清单。
4. 是否重命名。
5. 是否更新 `文献.md` 和 `文献引用.txt`。
6. 是否完成逐篇分析。
7. 影响了哪些方向报告。
8. 后续待办。

## 11. 代码和复现资源处理

如果新增论文有官方代码：

1. 在 `source/` 下建立对应目录或记录仓库地址。
2. 更新 `source/README.md`。
3. 更新 `综合分析/07_代码对照总表.md`。
4. 在逐篇分析中写明入口脚本、模型文件、数据预处理和复现风险。

如果暂无代码，也要标注“未发现”，避免后续重复查找。

## 12. 最终质检清单

补充完成后，逐项检查：

- [ ] `paper/` 数量正确。
- [ ] 新增 PDF 均可打开。
- [ ] 新增 PDF 已按 DOI 或稳定题名命名。
- [ ] 无重复 DOI 或重复论文。
- [ ] `文献.md` 总数、编号、PDF 链接、DOI 链接正确。
- [ ] `文献引用.txt` 与 `文献.md` 编号一致。
- [ ] `综合分析/论文分析总表.csv` 已新增条目。
- [ ] 强相关论文已生成逐篇精读。
- [ ] 方向分析文件已吸收新增论文结论。
- [ ] 论文初稿已补相关工作、方法依据或实验基线。
- [ ] README 或统计报告中的数量口径已同步。
- [ ] 临时文件、下载残留和重复文件已清理。

## 13. 本次补充后的状态

本次 `paper/` 数量已从 850 增至 858，新增论文主要服务“可信开放集检测”方向。当前处理状态如下：

1. 已将 8 篇新增 PDF 统一重命名为 DOI/稳定 arXiv 形式。
2. 已在 `文献.md` 和 `文献引用.txt` 中追加 `[851]` 到 `[858]`。
3. 已为 8 篇新增论文补入 `综合分析/论文分析总表.csv`。
4. 已对 8 篇新增论文生成规则版中文解析。
5. 已为 8 篇新增论文生成全文文本缓存。
6. 已对 [851]-[858] 完成 GPT5.5 深度精读。
7. 已更新 `可信开放集检测方向整体方案分析.md`，把“外部检索补充来源”改为“已补入本地文献”。
8. 已更新论文初稿的相关工作和方法章节，使 evidence、OpenMax、Energy、Mahalanobis、Calibration 都有正式引用支撑。
9. PDF 命名与引用路径已同步完成。
