Clean the whole source slide into a source-identity-preserving background.

Use the provided source image as the only reference. Preserve:
- original aspect ratio and composition
- background color/texture and all non-text visual structure
- relative positions, spacing, linework, panel edges, borders, and shadows
- overall dashboard-like code-tree layout

Strictly remove all readable text, labels, code words, numeric annotations, and any
text-like content that will be rebuilt as native PPT text boxes.
Do not invent new content, logos, charts, colors, icons, or objects.
Do not shift, recolor, blur, or simplify the source content.

Keep the result clean and source-faithful so removed text regions blend naturally
into surrounding background and background structure remains unchanged.
