You are provided the source slide image.
Create a clean base image that keeps the full diagram geometry, color, lighting, and layout, but remove all readable text labels, symbols, numbers, and captions that should become editable text.
Preserve all non-text graphics including boxes, lines, arrows, and shapes.
Do not redraw or redesign the layout; return a direct source-consistent background only.
Do not add any new objects.
